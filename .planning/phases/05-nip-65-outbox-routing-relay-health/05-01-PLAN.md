---
phase: 05-nip-65-outbox-routing-relay-health
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0004_pubkey_relays.sql
  - src/ingest/mod.rs
  - src/ingest/relay_list.rs
  - src/store/relays.rs
  - src/store/mod.rs
  - tests/common/mod.rs
  - tests/relay_list.rs
  - tests/relay_lists_store.rs
  - tests/nip65_fallback.rs
  - .sqlx/
autonomous: true
requirements: [RELAY-05]
must_haves:
  truths:
    - "A winning kind:10002 event's r-tag relay URLs and read/write markers are extracted into a ValidatedRelayList (bare r-tag = both)"
    - "apply_relay_list persists a pubkey's relays as a newest-wins full replace in one transaction"
    - "lookup_write_relays returns only relays with marker IN ('write','both') for a pubkey"
    - "A fresh database migrated from empty reaches the 0004 schema, and re-running migrations is a no-op"
  artifacts:
    - path: "migrations/0004_pubkey_relays.sql"
      provides: "pubkey_relays table (pubkey_id, url, marker CHECK, seen_at) + per-pubkey index, additive/idempotent, INTERNAL not contract"
      contains: "CREATE TABLE IF NOT EXISTS pubkey_relays"
    - path: "src/ingest/relay_list.rs"
      provides: "extract_relay_pairs via nip65::extract_relay_list (None->both, Read->read, Write->write)"
    - path: "src/ingest/mod.rs"
      provides: "ValidatedRelayList type (companion to ValidatedFollowList)"
      contains: "ValidatedRelayList"
    - path: "src/store/relays.rs"
      provides: "apply_relay_list (txn delete-not-in-set full replace) + lookup_write_relays"
      exports: ["apply_relay_list", "lookup_write_relays"]
    - path: "tests/nip65_fallback.rs"
      provides: "named-ignored Wave 0 scaffold for fallback recovery/miss/deadlock tests"
    - path: "tests/common/mod.rs"
      provides: "relay-URL-aware + error-injecting ScriptedGraph + relay_list_event fixture"
  key_links:
    - from: "src/ingest/relay_list.rs"
      to: "nostr_sdk::nip65::extract_relay_list"
      via: "r-tag extraction on the winning kind:10002 event"
      pattern: "nip65::extract_relay_list"
    - from: "src/store/relays.rs"
      to: "pubkey_relays table"
      via: "DELETE then INSERT in one tx; SELECT marker IN ('write','both')"
      pattern: "pubkey_relays"
---

<objective>
Lay the RELAY-05 storage + extraction foundation: migration 0004 (`pubkey_relays`), the `ValidatedRelayList` type and r-tag extraction via the built-in NIP-65 helper, and the `apply_relay_list` / `lookup_write_relays` store functions. Also build the relay-URL-aware + error-injecting `ScriptedGraph` seam and the kind:10002 fixture that the fallback (05-03) and health-routing (05-04) plans depend on, plus the named-ignored `tests/nip65_fallback.rs` scaffold.

Purpose: Without persisted write relays there is no fallback target (RELAY-05), and without the URL-aware/error-injecting test seam the fallback and health-routing plans cannot be tested. This plan must land first because every other Phase 5 plan reads its outputs.
Output: migration 0004, `src/ingest/relay_list.rs`, `ValidatedRelayList` in `src/ingest/mod.rs`, `src/store/relays.rs`, extended `tests/common/mod.rs`, extended `tests/relay_list.rs`, new `tests/relay_lists_store.rs`, and the named-ignored scaffold `tests/nip65_fallback.rs`. Regenerated committed `.sqlx/` metadata. (NOTE: `tests/relay_health.rs` is created by 05-02, not here, to keep file ownership disjoint within Wave 1.)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-nip-65-outbox-routing-relay-health/05-RESEARCH.md
@.planning/phases/05-nip-65-outbox-routing-relay-health/05-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0004 pubkey_relays (additive/idempotent, INTERNAL)</name>
  <files>migrations/0004_pubkey_relays.sql</files>
  <read_first>
    - migrations/0002_frontier.sql (named-CHECK lines 25-27, IF NOT EXISTS, INTERNAL: COMMENT ON lines 55-58)
    - migrations/0003_staleness.sql (additive header lines 1-23, index-only convention)
    - migrations/0001_graph_schema.sql (note: init migration is 0001_graph_schema.sql NOT 0001_init.sql; pubkeys.id surrogate bigint PK)
    - 05-RESEARCH.md Pattern 1 (the exact table/index/PK/COMMENT shape)
  </read_first>
  <action>
    Create migrations/0004_pubkey_relays.sql mirroring 0002/0003 conventions exactly. Lead with the additive/idempotent header comment (re-running is a no-op; sqlx wraps each migration in a transaction). Create table: `CREATE TABLE IF NOT EXISTS pubkey_relays (pubkey_id BIGINT NOT NULL REFERENCES pubkeys(id), url TEXT NOT NULL, marker TEXT NOT NULL CONSTRAINT pubkey_relays_marker_check CHECK (marker IN ('read','write','both')), seen_at TIMESTAMPTZ NOT NULL, PRIMARY KEY (pubkey_id, url))`. Add `CREATE INDEX IF NOT EXISTS pubkey_relays_pubkey_idx ON pubkey_relays (pubkey_id)`. Add `COMMENT ON TABLE pubkey_relays IS 'INTERNAL: per-pubkey NIP-65 (kind:10002) advertised relays for outbox-style fallback routing (RELAY-05). Newest-wins replaced per pubkey on each winning kind:10002. NOT part of the public contract.'`. This table implements the RELAY-05 storage prerequisite. Do NOT add pubkey_relays to the pubkey_freshness contract view or any other contract view — it is internal routing bookkeeping (GRAPH-04).
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo test --test relay_lists_store migration_0004_idempotent -- --test-threads=2</automated>
  </verify>
  <acceptance_criteria>
    - File migrations/0004_pubkey_relays.sql exists with `CREATE TABLE IF NOT EXISTS pubkey_relays` and named constraint `pubkey_relays_marker_check`.
    - Index `pubkey_relays_pubkey_idx` created with IF NOT EXISTS.
    - `COMMENT ON TABLE pubkey_relays` contains the literal token `INTERNAL:` and `NOT part of the public contract`.
    - grep confirms pubkey_relays does NOT appear in any contract view: `grep -rni 'create .*view' migrations/ | grep -i pubkey_relays` returns nothing.
    - Migration idempotency test (Task 4 body) runs migrations twice against a fresh testcontainers DB and the second run is a no-op.
  </acceptance_criteria>
  <done>Migration 0004 exists, is additive/idempotent, creates pubkey_relays + index + INTERNAL comment, and is absent from all contract views.</done>
</task>

<task type="auto">
  <name>Task 2: ValidatedRelayList + r-tag extraction via nip65 helper</name>
  <files>src/ingest/mod.rs, src/ingest/relay_list.rs</files>
  <read_first>
    - src/ingest/mod.rs lines 45-87 (ValidatedFollowList struct + from_event), lines 65-69 (timestamp_to_datetime), lines 109-111 (the documented seam where r-tags are dropped), lines 152-156 (pick_winner reuse)
    - 05-RESEARCH.md Pattern 2 (marker_of: None->both, Read->read, Write->write; RelayUrl::as_str_without_trailing_slash) and Pitfall 2 (bare r-tag means BOTH)
    - 05-PATTERNS.md "src/ingest/mod.rs + src/ingest/relay_list.rs" section
  </read_first>
  <action>
    Add a `ValidatedRelayList` struct to src/ingest/mod.rs as a companion to `ValidatedFollowList`, with fields `{ pubkey: PublicKey, event_id: EventId, created_at: DateTime<Utc>, relays: Vec<(String, &'static str)> }` (url + marker). Reuse the existing `timestamp_to_datetime` for `created_at`; do not re-derive the conversion. Register `pub mod relay_list;` in src/ingest/mod.rs. In the new src/ingest/relay_list.rs, write `pub fn extract_relay_pairs(event: &Event) -> Vec<(String, &'static str)>` that uses `nostr_sdk::nip65::extract_relay_list(event)` and maps each `(&RelayUrl, &Option<RelayMetadata>)` to `(url.as_str_without_trailing_slash().to_string(), marker_of(meta))` where a private `fn marker_of(meta: &Option<RelayMetadata>) -> &'static str` returns `Some(Read)=>"read"`, `Some(Write)=>"write"`, `None=>"both"`. Add `pub fn from_event(event: &Event) -> ValidatedRelayList` building the struct from the winning event's pubkey/id/created_at plus `extract_relay_pairs`. NEVER hand-roll tag parsing — use the built-in nip65 helper (CLAUDE.md). pick_winner stays reused unchanged for kind:10002 (INGEST-05).
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo test --test relay_list extract -- --test-threads=2</automated>
  </verify>
  <acceptance_criteria>
    - `web_of_trust::ingest::ValidatedRelayList` exists with fields pubkey, event_id, created_at, relays.
    - `web_of_trust::ingest::relay_list::extract_relay_pairs` exists and is `pub`.
    - grep confirms the helper is used, not hand-rolled: `grep -n 'nip65::extract_relay_list' src/ingest/relay_list.rs` returns a match; `grep -c 'as_vec\|TagKind::single_letter' src/ingest/relay_list.rs` returns 0.
    - Extended tests/relay_list.rs asserts: a bare r-tag extracts as marker "both"; an `r url read` tag as "read"; an `r url write` tag as "write"; URLs are normalized without trailing slash.
    - `SQLX_OFFLINE=true cargo build` succeeds.
  </acceptance_criteria>
  <done>ValidatedRelayList and extract_relay_pairs exist, use the built-in nip65 helper, and the bare/read/write marker mapping is asserted green in tests/relay_list.rs.</done>
</task>

<task type="auto">
  <name>Task 3: store/relays.rs apply_relay_list + lookup_write_relays + .sqlx regen</name>
  <files>src/store/relays.rs, src/store/mod.rs, .sqlx/</files>
  <read_first>
    - src/store/follows.rs (apply_follow_list lines 94-139: pool.begin -> DELETE -> INSERT ON CONFLICT DO NOTHING -> commit; module header lines 1-10; chrono import line 14)
    - src/store/mod.rs lines 12-13 (pub mod follows; pub mod pubkeys; — register pub mod relays;)
    - src/error.rs line 15 (StoreError::Sqlx #[from] sqlx::Error gives ? on begin/execute)
    - 05-RESEARCH.md Pattern 3 (exact apply_relay_list + lookup_write_relays SQL) and Pitfall 6 (.sqlx regen with --all-targets)
  </read_first>
  <action>
    Create src/store/relays.rs with a phase-ID + RELAY-05 doc header mirroring follows.rs. Implement `pub async fn apply_relay_list(pool: &PgPool, pubkey_id: i64, relays: &[(String, &str)], seen_at: DateTime<Utc>) -> Result<(), StoreError>`: `let mut tx = pool.begin().await?;` then `sqlx::query!("DELETE FROM pubkey_relays WHERE pubkey_id = $1", pubkey_id).execute(&mut *tx).await?;` then loop `sqlx::query!("INSERT INTO pubkey_relays (pubkey_id, url, marker, seen_at) VALUES ($1, $2, $3, $4) ON CONFLICT (pubkey_id, url) DO NOTHING", pubkey_id, url, *marker, seen_at).execute(&mut *tx).await?;` then `tx.commit().await?; Ok(())`. This is a full-replace newest-wins (not an edge diff — relay rows per pubkey are tiny, per RESEARCH A2). Implement `pub async fn lookup_write_relays(pool: &PgPool, pubkey_id: i64) -> Result<Vec<String>, StoreError>` using `sqlx::query_scalar!("SELECT url FROM pubkey_relays WHERE pubkey_id = $1 AND marker IN ('write','both')", pubkey_id).fetch_all(pool).await?` — marker `both` MUST be selected (Pitfall 2: a bare r-tag advertises both read and write). Register `pub mod relays;` in src/store/mod.rs. After the macros compile against a live DB, regenerate offline metadata with `cargo sqlx prepare -- --all-targets` (lib-only prunes integration metadata — Phase 4 deviation) and commit the resulting .sqlx/ files.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo test --test relay_lists_store apply_relay_list -- --test-threads=2</automated>
  </verify>
  <acceptance_criteria>
    - `web_of_trust::store::relays::apply_relay_list` and `lookup_write_relays` exist and are `pub`.
    - apply_relay_list opens a transaction, DELETEs by pubkey_id, INSERTs each pair with ON CONFLICT DO NOTHING, commits (grep `pool.begin` and `tx.commit` in src/store/relays.rs).
    - lookup_write_relays SQL contains `marker IN ('write','both')` (grep confirms 'both' present).
    - .sqlx/ contains query metadata for the new DELETE/INSERT/SELECT queries; `SQLX_OFFLINE=true cargo build` succeeds with no live DB.
    - tests/relay_lists_store.rs apply_relay_list test (Task 4 body) proves a second winning relay list deletes the prior rows (newest-wins replace) over a fresh testcontainers DB.
  </acceptance_criteria>
  <done>apply_relay_list (transactional full replace) and lookup_write_relays (write+both) exist, .sqlx is regenerated/committed, and SQLX_OFFLINE build is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wave 0 test seams — URL-aware/error-injecting ScriptedGraph, fixtures, nip65 scaffold, store tests</name>
  <files>tests/common/mod.rs, tests/relay_list.rs, tests/relay_lists_store.rs, tests/nip65_fallback.rs</files>
  <read_first>
    - tests/common/mod.rs lines 150-188 (ScriptedGraph Arc-backed Send+Clone fetch_fn), lines 66-78 (signed_event EventBuilder+sign idiom), lines 193-197 (follows_event fixture), line 26 (RelayError import)
    - tests/relay_list.rs lines 14-43 (newest_relay_list_wins offline #[test] shape)
    - tests/daemon_config.rs (offline-unit test shape) and 05-RESEARCH.md Validation Architecture "Wave 0 Gaps"
    - 05-VALIDATION.md (per-task verification map, Wave 0 requirements)
  </read_first>
  <behavior>
    - migration_0004_idempotent: running migrations twice on a fresh DB is a no-op and pubkey_relays exists.
    - apply_relay_list newest-wins: persist relays for a pubkey, then persist a different set; only the second set remains.
    - lookup_write_relays returns write+both markers, excludes read-only.
    - ScriptedGraph models "author absent on curated relay A, present on write relay B" (relay-URL-aware) and can inject Err(RelayError::FetchTimeout(url)) for a designated relay.
    - relay_list_event(author_seed, &[(url, marker)]) builds a real kind:10002 event whose r-tags extract back to the same pairs.
  </behavior>
  <action>
    Extend tests/common/mod.rs ScriptedGraph so its inner store is relay-URL-aware (model `author -> HashMap<relay_url, Vec<Event>>` or `(relay_url, author) -> Vec<Event>`) and add an error-injection variant: a registered relay_url returns `Err(RelayError::FetchTimeout(url))` (and a Client-error variant) from the relay-aware fetch closure. Keep ScriptedGraph `Send + Clone + Sync + 'static`, Arc-backed, since it crosses tokio::spawn in the daemon loop. Preserve the existing union `fetch_fn()` behavior for callers that ignore relay URL (back-compat). Add a `relay_list_event(author_seed: u64, relays: &[(&str, &str)]) -> Event` fixture (sibling of follows_event) building a kind:10002 event via `EventBuilder::new(Kind::RelayList, "").tags(...)` with real r-tags + read/write markers, signed via the existing signed_event idiom. Extend tests/relay_list.rs with the r-tag extraction asserts named in Task 2 acceptance (bare->both, read, write, trailing-slash normalization) as offline `#[test]`s. Create tests/relay_lists_store.rs (testcontainers, uses common::fresh_db) with the three behaviors above as real (non-ignored) tests: `migration_0004_idempotent`, `apply_relay_list` newest-wins replace, `lookup_write_relays_write_and_both`. Create the named-ignored Wave 0 scaffold tests/nip65_fallback.rs (functions `fallback_recovers_via_write_relay`, `fallback_miss_stamps_not_found`, `no_deadlock_single_permit`, each `#[ignore = "Wave 0 scaffold; body lands in 05-03/05-04"]`). DB suites run with `-- --test-threads=2`; on a testcontainers container/port flake, re-run the suite once. (tests/relay_health.rs is NOT created here — 05-02 owns it.)
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo test --test relay_lists_store --test relay_list -- --test-threads=2</automated>
  </verify>
  <acceptance_criteria>
    - ScriptedGraph exposes a relay-URL-aware fetch closure and an error-injection path returning RelayError::FetchTimeout; it remains Send + Clone (grep `Send` / `Clone` on the impl).
    - `relay_list_event` fixture exists in tests/common/mod.rs; a round-trip test (build event -> extract_relay_pairs) yields the input pairs.
    - tests/relay_lists_store.rs has migration_0004_idempotent, apply_relay_list newest-wins, lookup_write_relays_write_and_both — all NON-ignored and green over testcontainers.
    - tests/nip65_fallback.rs exists with the three named functions, each marked `#[ignore = "Wave 0 scaffold..."]` (so `cargo test` compiles them; they do not run yet).
    - `cargo test --test nip65_fallback -- --test-threads=2` reports the scaffold tests as ignored (not failed).
  </acceptance_criteria>
  <done>The URL-aware/error-injecting ScriptedGraph + relay_list_event fixture exist, relay_lists_store.rs store/migration tests are green, and the nip65_fallback Wave 0 scaffold compiles.</done>
</task>

</tasks>

<verification>
- `SQLX_OFFLINE=true cargo build` and `SQLX_OFFLINE=true cargo build --tests` succeed with committed .sqlx metadata (no live DB).
- `SQLX_OFFLINE=true cargo test --test relay_list --test relay_lists_store -- --test-threads=2` green (re-run once on a testcontainers flake).
- `cargo test --test nip65_fallback -- --test-threads=2` compiles; scaffold tests report ignored.
- pubkey_relays appears in no contract view (grep gate in Task 1).
</verification>

<success_criteria>
- Migration 0004 brings a fresh DB to the pubkey_relays schema and re-running is a no-op (RELAY-05 storage prerequisite).
- A winning kind:10002 event's r-tags (bare=both, read, write) extract into a ValidatedRelayList and persist via apply_relay_list as a newest-wins replace; lookup_write_relays returns write+both relays.
- The relay-URL-aware + error-injecting ScriptedGraph seam and the nip65_fallback Wave 0 scaffold exist for the downstream fallback + health-routing plans.
</success_criteria>

<output>
Create `.planning/phases/05-nip-65-outbox-routing-relay-health/05-01-SUMMARY.md` when done.
</output>
