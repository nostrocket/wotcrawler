---
phase: 05-nip-65-outbox-routing-relay-health
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - src/relay/fetch.rs
  - src/daemon/mod.rs
  - src/daemon/observe.rs
  - src/daemon/sampler.rs
  - ops/grafana-dashboard.json
  - tests/nip65_fallback.rs
autonomous: true
requirements: [RELAY-06]
must_haves:
  truths:
    - "The daemon fan-out skips relays whose health score is below relay_health_threshold unless a probe is due, then records the outcome to re-admit recovered relays"
    - "Per-relay concurrency is gated by a fixed-size Semaphore + an in-use count against max(1, round(per_relay_concurrency * score)), so a degraded relay receives less traffic than a healthy one"
    - "Resource acquisition order is fixed everywhere: global crawl permit -> per-relay permit -> GCRA token -> fetch (deadlock-safe)"
    - "Health is recorded at the Ok/Err arms of the per-relay fetch (success+latency, timeout, connect-failure)"
    - "A per-relay health gauge (curated set only), nip65_recovered counter, and per-relay concurrency-in-use gauge are exported; Grafana panels render them"
  artifacts:
    - path: "src/daemon/mod.rs"
      provides: "health-driven fan-out: skip-below-threshold+probe, per-relay Semaphore admission gate, deadlock-safe ordering, health capture at fetch arms; RelayHealthRegistry built+threaded into fan-out/notice consumer/sampler; process_batch called with fallback_fetch + fallback config"
      contains: "RelayHealthRegistry"
    - path: "src/relay/fetch.rs"
      provides: "health record_success(latency)/record_timeout/record_connect_failure mapped from RelayError variants at the per-relay outcome (threaded health, no behavior change to pagination)"
    - path: "src/daemon/observe.rs"
      provides: "METRIC_RELAY_HEALTH, METRIC_NIP65_RECOVERED, METRIC_RELAY_CONCURRENCY consts"
      contains: "METRIC_RELAY_HEALTH"
    - path: "src/daemon/sampler.rs"
      provides: "labeled per-relay health gauge over the bounded curated set + per-relay concurrency-in-use gauge"
    - path: "ops/grafana-dashboard.json"
      provides: "panels for relay_health (labeled), nip65_recovered_total rate, per-relay concurrency-in-use"
  key_links:
    - from: "src/daemon/mod.rs fetch_union fan-out"
      to: "RelayHealthRegistry::route_allowed + permits/in_use + record_*"
      via: "skip-below-threshold + per-relay semaphore admission + Ok/Err health capture"
      pattern: "route_allowed"
    - from: "src/daemon/mod.rs"
      to: "process_batch fallback_fetch"
      via: "the fan-out passes the live-Client fallback_fetch + relay_list_fetch closures + fallback config into the crawl loop"
      pattern: "fallback_fetch"
---

<objective>
Make the relay health score visibly drive routing and per-relay concurrency (RELAY-06): in the daemon fan-out, skip relays below `relay_health_threshold` (with periodic probe re-admission), gate per-relay concurrency by a fixed-size Semaphore + health-scaled in-use count, capture health at the per-relay fetch Ok/Err arms, and observe the deadlock-safe acquisition order. Reuse the `RelayHealthRegistry` that 05-02 already constructs at daemon start (extend that binding; do NOT create a second registry) and thread it into the fan-out, the notice consumer (from 05-02), and the sampler. Provide the live-Client `fallback_fetch` + on-demand `relay_list_fetch` closures to `process_batch` (from 05-03). Export the new metrics + Grafana panels.

Purpose: RELAY-06's success criterion is that the score visibly drives routing and per-relay concurrency so a degraded relay receives less traffic than a healthy one. This is the final wiring plan; it consumes the health registry (05-02) and the fallback seam (05-03) and connects them at the only routing site.
Output: health-driven fan-out + per-relay Semaphore in `src/daemon/mod.rs`; health capture in `src/relay/fetch.rs`; the 3 metric-name consts in `src/daemon/observe.rs`; the labeled curated-only health gauge + concurrency-in-use gauge in `src/daemon/sampler.rs`; Grafana panels; the green `no_deadlock_single_permit` test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-nip-65-outbox-routing-relay-health/05-RESEARCH.md
@.planning/phases/05-nip-65-outbox-routing-relay-health/05-PATTERNS.md
@.planning/phases/05-nip-65-outbox-routing-relay-health/05-02-SUMMARY.md
@.planning/phases/05-nip-65-outbox-routing-relay-health/05-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Health capture at the per-relay fetch Ok/Err arms</name>
  <files>src/relay/fetch.rs</files>
  <read_first>
    - src/relay/fetch.rs lines 238-254 (fetch_window_with_deadline Instant::now() latency idiom), lines 318-366 (fetch_complete_with_timeout — the per-relay Ok/Err outcome site)
    - src/error.rs lines 38-62 (RelayError variants: FetchTimeout(String) at 61, Client(#[from] nostr_sdk::client::Error) at 42)
    - src/relay/health.rs RelayHealthRegistry::{record_success, record_timeout, record_connect_failure} (from 05-02)
    - 05-RESEARCH.md "Recording health at the fetch arms" + A4 (map Client(_) -> connect-failure; FetchTimeout -> timeout), 05-PATTERNS.md "src/relay/fetch.rs" section
  </read_first>
  <action>
    Thread an `Option<&RelayHealthRegistry>` (or `&RelayHealthRegistry`, matching how the GCRA registry is threaded) into the per-relay fetch outcome so health is recorded at the Ok/Err arms WITHOUT changing pagination behavior. Measure latency with `std::time::Instant` around the per-relay fetch (the seam already uses this idiom). On `Ok(events)` -> `health.record_success(relay_url, t0.elapsed())`. On `Err(RelayError::FetchTimeout(_))` -> `health.record_timeout(relay_url)`. On `Err(RelayError::Client(_))` and any other `Err(_)` -> `health.record_connect_failure(relay_url)` (RESEARCH A4: nostr-sdk exposes no clean per-relay connect-status sampler, so Client errors map to connect-failure). RECOMMENDED placement per RESEARCH/PATTERNS: do the classification at the daemon fan-out call site (Task 2) where `&health` is already in scope, OR thread `health` into fetch_complete_with_timeout exactly as `registry` is threaded today — pick whichever keeps the smaller signature delta; if classification is done at the call site in Task 2, this task only ensures the latency-measuring seam and the RelayError variant surface are usable there. Do not alter pagination, timeout, or GCRA behavior.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo build --tests</automated>
  </verify>
  <acceptance_criteria>
    - Health is recorded for every per-relay fetch outcome: success (with latency), timeout, connect-failure — keyed by the individual relay_url (grep `record_success`/`record_timeout`/`record_connect_failure` reachable from the fetch path or the fan-out call site).
    - RelayError::FetchTimeout maps to record_timeout; RelayError::Client maps to record_connect_failure.
    - Pagination/timeout/GCRA behavior is unchanged (no edits inside the page-back loop logic).
    - `SQLX_OFFLINE=true cargo build --tests` green.
  </acceptance_criteria>
  <done>Per-relay fetch outcomes (success+latency, timeout, connect-failure) feed RelayHealthRegistry without altering fetch/pagination behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Health-driven routing + per-relay Semaphore admission + deadlock-safe order + fetch-arm capture</name>
  <files>src/daemon/mod.rs</files>
  <read_first>
    - src/daemon/mod.rs lines 163-168 (RateLimiterRegistry built once+Arc), 168 (spawn_notice_consumer — already takes Arc<RelayHealthRegistry> from 05-02, and the `health` binding is already constructed here by 05-02's wiring patch), lines 176-222 (the fetch_union closure — the only routing site), lines 201-218 (the static uniform per-relay loop to modify), lines 251-268 (run_daemon_loop spawn passing fetch_union), lines 270-279 (sampler spawn)
    - src/daemon/loop_.rs line 98 (global crawl Semaphore::new(concurrency)), lines 137-140 (global permit acquire_owned BEFORE spawn — the deadlock-order anchor)
    - src/relay/health.rs RelayHealthRegistry::{new, route_allowed, mark_attempt, permits, in_use, incr_in_use, decr_in_use, record_*} (from 05-02)
    - 05-RESEARCH.md Pattern 7 (skip+probe), Pattern 8 (per-relay Semaphore + in-use gate, fixed size, no shrink), Pitfall 1 (acquisition order), Pitfall 5 (Semaphore cannot shrink — use in-use gate)
    - 05-PATTERNS.md "src/daemon/mod.rs" + "src/daemon/loop_.rs" sections
  </read_first>
  <action>
    Reuse the `let health = Arc::new(RelayHealthRegistry::new(cfg.health_alpha));` binding that 05-02 ALREADY constructs next to the RateLimiterRegistry (lines 163-168) — do NOT construct a second registry; if for any reason it is absent (e.g. 05-02 used the const fallback), confirm it now reads `cfg.health_alpha`. Clone the existing `health` into the fetch_union closure for route + record + per-relay admission. Create a per-relay Semaphore map `Arc<HashMap<String, Arc<tokio::sync::Semaphore>>>` sized `Semaphore::new(cfg.per_relay_concurrency)` per curated relay url, built once and cloned into the closure (tokio::Semaphore cannot shrink — Pitfall 5; the health scaling is done by the in-use gate, not by resizing). In the per-relay loop (lines 201-218): (1) `if !health.route_allowed(relay_url, cfg.relay_health_threshold) { continue; }` to skip degraded relays unless a probe is due (Pattern 7), and call `health.mark_attempt(relay_url)` when proceeding; (2) enforce the health-scaled admission gate: skip/await this relay when `health.in_use(relay_url) >= health.permits(relay_url, cfg.per_relay_concurrency)` (Pattern 8), then acquire the per-relay Semaphore permit; (3) acquire the GCRA token (existing `registry`/fetch path); (4) fetch. ACQUISITION ORDER, fixed EVERYWHERE (Pitfall 1): global crawl permit (already acquired in loop_.rs:137 before the batch is spawned) -> per-relay permit -> GCRA token -> fetch. NEVER acquire a global permit while holding a per-relay permit. Wrap incr_in_use/decr_in_use around the per-relay fetch (decrement on all exit paths). Record health at the Ok/Err arms here (Task 1 classification): Ok -> record_success(latency); Err(FetchTimeout) -> record_timeout; Err(other) -> record_connect_failure. PRESERVE the existing `?`-on-per-relay-error requeue semantic (the batch requeues rather than being treated complete) but record health BEFORE propagating. Document the acquisition order in a code comment at the fan-out. NOTE: building the live-Client fallback_fetch/relay_list_fetch closures and threading them into process_batch is Task 3 — keep this task to routing + admission + capture only.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo build --tests</automated>
  </verify>
  <acceptance_criteria>
    - The `health` registry is the SAME binding constructed by 05-02 (grep shows a single `RelayHealthRegistry::new` in src/daemon/mod.rs, not two), reading `cfg.health_alpha`.
    - The fan-out skips a relay when `route_allowed` is false (grep `route_allowed` in src/daemon/mod.rs) and re-admits via probe (mark_attempt called).
    - A per-relay Semaphore map exists (fixed size per_relay_concurrency) plus an in-use gate against `permits(relay, per_relay_concurrency)`; in_use is incremented/decremented around each per-relay fetch on all exit paths.
    - A code comment at the fan-out states the order: global crawl permit -> per-relay permit -> GCRA token -> fetch.
    - Health recorded at Ok/Err arms before any `?` propagation (per-relay error still requeues the batch).
    - `SQLX_OFFLINE=true cargo build --tests` green.
  </acceptance_criteria>
  <done>The fan-out is health-driven (skip+probe), per-relay concurrency scales with health via a fixed Semaphore + in-use gate, the acquisition order is fixed + documented, and health is captured at the fetch arms — reusing the 05-02 registry binding.</done>
</task>

<task type="auto">
  <name>Task 3: Build the live-Client fallback_fetch + relay_list_fetch closures and thread them into process_batch</name>
  <files>src/daemon/mod.rs</files>
  <read_first>
    - src/daemon/mod.rs lines 176-222 (fetch_union closure — the GCRA-gated per-relay fetch path the fallback closure reuses), lines 251-268 (run_daemon_loop spawn — where process_batch is invoked / its params are threaded)
    - src/crawl/apply.rs process_batch (from 05-03: now needs fallback_enabled, nip65_max_write_relays, &RelayHealthRegistry, fallback_fetch, relay_list_fetch)
    - src/relay/fetch.rs fetch_complete_with_timeout (the per-relay fetch the live closures call)
    - src/relay/health.rs RelayHealthRegistry (the same `health` binding from Task 2 / 05-02)
    - 05-RESEARCH.md Pattern 4 (the closures the crawl layer expects), Pitfall 4 (on-demand kind:10002 is a plain curated fetch, not recursive)
    - 05-PATTERNS.md "src/daemon/mod.rs" section
  </read_first>
  <action>
    Build the live-Client `fallback_fetch: Fn(PublicKey, Vec<String>) -> Fut` closure (fetch kind-3 from the given write-relay URLs via fetch_complete_with_timeout through the same GCRA-gated path used by the fan-out) and the on-demand `relay_list_fetch: Fn(PublicKey) -> Fut` closure (plain curated kind:10002 fetch — NOT routed back through the kind-3 fallback, Pitfall 4). These closures own the only references to the live `Client` so the crawl layer (05-03 apply.rs) stays Client-free. Thread these two closures + `cfg.nip65_fallback_enabled`, `cfg.nip65_max_write_relays`, and `Arc::clone(&health)` (the same registry from Task 2) through run_daemon_loop into `process_batch` (matching the 05-03 signature). Do not duplicate the routing/admission logic from Task 2 — these closures are the live-fetch implementations the already-wired fan-out and crawl loop call into.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo build --tests</automated>
  </verify>
  <acceptance_criteria>
    - A live-Client `fallback_fetch` closure (signature `Fn(PublicKey, Vec<String>) -> Fut`) and a `relay_list_fetch` closure (plain curated kind:10002 fetch) are constructed in src/daemon/mod.rs (grep `fallback_fetch` + `relay_list_fetch`).
    - process_batch is invoked with fallback_enabled, nip65_max_write_relays, &RelayHealthRegistry (the Task 2 `health` binding), the live-Client fallback_fetch, and the relay_list_fetch closures.
    - The closures own the live Client references; src/crawl/apply.rs remains Client-free (unchanged from 05-03).
    - `SQLX_OFFLINE=true cargo build --tests` green.
  </acceptance_criteria>
  <done>The live-Client fallback_fetch + on-demand relay_list_fetch closures are built and threaded into process_batch with the fallback config and the shared health registry.</done>
</task>

<task type="auto">
  <name>Task 4: Metric-name consts + labeled curated-only health gauge + concurrency-in-use gauge</name>
  <files>src/daemon/observe.rs, src/daemon/sampler.rs</files>
  <read_first>
    - src/daemon/observe.rs lines 44-51 (doc block: aggregate gauges, NO per-pubkey labels — cardinality), lines 53-73 (METRIC_* pub const block)
    - src/daemon/sampler.rs lines 116-164 (sample_gauges), lines 154-162 (relay-health block iterating the bounded curated relay set), lines 242-244 (no-manual-_total counter idiom)
    - src/daemon/mod.rs lines 270-279 (sampler spawn — thread Arc<RelayHealthRegistry> as registry is threaded)
    - 05-RESEARCH.md Pitfall 7 (labeled gauge for CURATED set only; transient write relays tracked in-memory but NOT exported per-URL), 05-PATTERNS.md "observe.rs" + "sampler.rs" sections
  </read_first>
  <action>
    In src/daemon/observe.rs add three `pub const`s in the METRIC_* block: `METRIC_RELAY_HEALTH: &str = "relay_health"` (labeled gauge), `METRIC_NIP65_RECOVERED: &str = "nip65_recovered"` (counter reference — fired un-suffixed at the apply.rs site, exports as nip65_recovered_total), `METRIC_RELAY_CONCURRENCY: &str = "relay_concurrency_in_use"` (per-relay in-use gauge). Thread an `Arc<RelayHealthRegistry>` into `sample_gauges` exactly as `registry: Arc<RateLimiterRegistry>` is threaded (update the spawn site in src/daemon/mod.rs lines 270-279, passing `Arc::clone(&health)`). In the curated-relay loop of sample_gauges (the bounded `relays` iteration), add ONLY for each curated relay url: `metrics::gauge!(METRIC_RELAY_HEALTH, "relay" => r.clone()).set(health.score(r));` and `metrics::gauge!(METRIC_RELAY_CONCURRENCY, "relay" => r.clone()).set(health.in_use(r) as f64);`. Do NOT emit either labeled gauge for transient write relays (Pitfall 7 — unbounded cardinality / metric DoS); health for write relays stays in-memory for routing only. Keep gauges un-suffixed (the exporter adds _total only to counters).
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo build --tests</automated>
  </verify>
  <acceptance_criteria>
    - observe.rs defines METRIC_RELAY_HEALTH, METRIC_NIP65_RECOVERED, METRIC_RELAY_CONCURRENCY as pub const (grep in observe.rs).
    - sample_gauges takes an Arc<RelayHealthRegistry> and emits relay_health + relay_concurrency_in_use labeled by relay ONLY over the curated `relays` slice.
    - grep confirms the labeled gauges are emitted inside the curated-relay loop and NOT over write-relay URLs.
    - Metric names carry no manual _total suffix.
    - `SQLX_OFFLINE=true cargo build --tests` green.
  </acceptance_criteria>
  <done>The 3 metric consts exist and the labeled per-relay health + concurrency-in-use gauges are emitted over the bounded curated set only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Grafana panels + deadlock-safety test (un-ignore no_deadlock_single_permit)</name>
  <files>ops/grafana-dashboard.json, tests/nip65_fallback.rs</files>
  <read_first>
    - ops/grafana-dashboard.json (the relay_consecutive_failures/relay_active_count timeseries panel; counter-rate exprs using _total)
    - 05-PATTERNS.md "ops/grafana-dashboard.json" section (panel skeleton; gauge expr no _total, counter expr DOES use _total in PromQL)
    - tests/nip65_fallback.rs no_deadlock_single_permit scaffold; tests/common/mod.rs URL-aware ScriptedGraph
    - 05-RESEARCH.md Validation Architecture (no_deadlock_single_permit row), Pitfall 1
  </read_first>
  <behavior>
    - no_deadlock_single_permit: a batch fanning out to multiple relays with per_relay_concurrency=1 completes (does not hang) — proving the global->per-relay->GCRA acquisition order is deadlock-free.
  </behavior>
  <action>
    Add three panels to ops/grafana-dashboard.json mirroring the existing panel shape: (1) per-relay health timeseries with `"expr": "relay_health"`, `"legendFormat": "{{relay}}"` (gauge, NO _total); (2) NIP-65 recovery rate with `"expr": "sum(rate(nip65_recovered_total[5m]))"` (counter — the exporter appends _total, so PromQL uses it even though the code fires `nip65_recovered`); (3) per-relay concurrency-in-use with `"expr": "relay_concurrency_in_use"`, `"legendFormat": "{{relay}}"` (gauge). Keep valid JSON (unique panel ids + gridPos). Then un-ignore and implement `no_deadlock_single_permit` in tests/nip65_fallback.rs: build a config/driver with `per_relay_concurrency=1` and a URL-aware ScriptedGraph scripting multiple relays, run the fan-out + process_batch path under a bounded `tokio::time::timeout`, and assert it COMPLETES within the timeout (a hang = deadlock fails the test). Run `-- --test-threads=2`; re-run once on a testcontainers flake.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo test --test nip65_fallback no_deadlock_single_permit -- --test-threads=2</automated>
  </verify>
  <acceptance_criteria>
    - ops/grafana-dashboard.json is valid JSON (parses) and contains panels with exprs `relay_health`, `sum(rate(nip65_recovered_total[5m]))`, and `relay_concurrency_in_use`.
    - no_deadlock_single_permit is no longer `#[ignore]` and passes: the multi-relay fan-out with per_relay_concurrency=1 completes within a bounded timeout.
    - `python3 -c "import json; json.load(open('ops/grafana-dashboard.json'))"` (or jq) succeeds.
  </acceptance_criteria>
  <done>Grafana renders the health gauge, recovery-rate, and per-relay concurrency panels, and the deadlock-safety test is green.</done>
</task>

</tasks>

<verification>
- `SQLX_OFFLINE=true cargo build --tests` green.
- `SQLX_OFFLINE=true cargo test --test nip65_fallback --test relay_health -- --test-threads=2` green (re-run once on flake).
- Full suite `SQLX_OFFLINE=true cargo test -- --test-threads=2` green before /gsd-verify-work (re-run once on testcontainers flake).
- ops/grafana-dashboard.json parses as JSON.
- Labeled gauges emitted for the curated set only (grep gate in Task 4).
</verification>

<success_criteria>
- The health score visibly drives routing (skip-below-threshold + periodic probe) and per-relay concurrency (fixed Semaphore + health-scaled in-use gate), so a degraded relay receives less traffic than a healthy one (RELAY-06).
- Health is captured at the per-relay fetch Ok/Err arms and the rate-limit NOTICE; the acquisition order is fixed and deadlock-free (proven at per_relay_concurrency=1).
- Per-relay health gauge (curated only), nip65_recovered counter, and per-relay concurrency-in-use are exported and rendered in Grafana.
</success_criteria>

<output>
Create `.planning/phases/05-nip-65-outbox-routing-relay-health/05-04-SUMMARY.md` when done.
</output>
