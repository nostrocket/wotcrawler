---
phase: 01-schema-data-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rust-toolchain.toml
  - Cargo.toml
  - .gitignore
  - src/lib.rs
  - src/error.rs
  - tests/common/mod.rs
autonomous: false
requirements: [GRAPH-01]
must_haves:
  truths:
    - "cargo build succeeds on a toolchain >= 1.94"
    - "The project compiles with sqlx 0.9, tokio, thiserror, anyhow, config as dependencies"
    - "An integration test can spin an ephemeral Postgres via testcontainers and obtain a connection URL"
  artifacts:
    - path: "rust-toolchain.toml"
      provides: "Pinned Rust toolchain >= 1.94 (sqlx 0.9 MSRV)"
      contains: "channel"
    - path: "Cargo.toml"
      provides: "Crate manifest with locked deps and rust-version = 1.94"
      contains: "sqlx"
    - path: "src/lib.rs"
      provides: "Crate root re-exporting store and error modules"
      min_lines: 2
    - path: "src/error.rs"
      provides: "thiserror StoreError enum"
      contains: "StoreError"
    - path: "tests/common/mod.rs"
      provides: "Shared testcontainers Postgres bootstrap helper returning a connection URL"
      contains: "Postgres"
  key_links:
    - from: "tests/common/mod.rs"
      to: "testcontainers_modules::postgres::Postgres"
      via: "ephemeral container start"
      pattern: "Postgres::default\\(\\)"
---

<objective>
Establish the greenfield Rust project: install/pin a toolchain >= 1.94 (sqlx 0.9 MSRV), create the Cargo manifest with the locked dependency set, scaffold the crate skeleton (lib root, error module), and provide the shared testcontainers Postgres bootstrap helper that every integration test in later plans depends on.

Purpose: Nothing in this phase can build or be tested until the toolchain and project scaffold exist. Research flagged this as a BLOCKING environment gap (cargo not installed; sqlx 0.9.0 declares MSRV Rust 1.94.0, contradicting CLAUDE.md's "1.84+"). This plan removes the blocker and lays every project precedent (crate layout, error handling, test bootstrap).
Output: A compiling Rust crate skeleton with pinned toolchain, locked deps, error type, and a reusable testcontainers Postgres fixture.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-schema-data-contract/01-CONTEXT.md
@.planning/phases/01-schema-data-contract/01-RESEARCH.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New files: `rust-toolchain.toml`, `Cargo.toml`, `.gitignore`, `src/lib.rs`, `src/error.rs`, `tests/common/mod.rs`.

New symbols:
- `StoreError` (enum, `src/error.rs`) — thiserror typed error for the store boundary
- `pg_container()` / test bootstrap fn (`tests/common/mod.rs`) — returns a started testcontainers Postgres handle and a `postgres://...` connection URL
- Module declarations `mod store;` and `mod error;` re-exported from `src/lib.rs`
</artifacts_produced>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm/install Rust toolchain >= 1.94</name>
  <decision>The build environment has no `cargo` on PATH. A toolchain >= 1.94 (sqlx 0.9.0 MSRV) must be available before any build work.</decision>
  <context>Research "Environment Availability" marks the Rust toolchain as a BLOCKING missing dependency with no fallback. sqlx 0.9.0 declares rust_version 1.94.0; CLAUDE.md's "1.84+" is superseded. The executor should first probe for an existing toolchain, then install via rustup only with operator consent (network + system install).</context>
  <read_first>
    - .planning/phases/01-schema-data-contract/01-RESEARCH.md (Environment Availability + Pitfall 1: MSRV)
  </read_first>
  <options>
    <option id="auto-detected">
      <name>Toolchain already present</name>
      <pros>If `rustc --version` / `cargo --version` reports >= 1.94, no install needed — proceed immediately.</pros>
      <cons>None.</cons>
    </option>
    <option id="install-rustup">
      <name>Install via rustup</name>
      <pros>Standard, reproducible; `rustup toolchain install 1.94` then pin. Operator approves the network/system install.</pros>
      <cons>Requires network access and modifies the operator's system; must be operator-approved.</cons>
    </option>
  </options>
  <action>Run `cargo --version` and `rustc --version`. If a toolchain >= 1.94 is present, record the version and resume autonomously. If absent or below 1.94, present the rustup install command (`rustup toolchain install 1.94.0`) and pause for operator approval before installing. Do NOT silently install without surfacing the command. After install/confirmation, capture the resolved version string for the SUMMARY.</action>
  <verify>
    <automated>cargo --version | grep -Eq 'cargo 1\.(9[4-9]|[0-9]{3})'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo --version` prints a version >= 1.94.0
    - `rustc --version` prints a version >= 1.94.0
    - The resolved toolchain version is recorded for the SUMMARY
  </acceptance_criteria>
  <resume-signal>Type "approved" to install via rustup, or confirm the detected toolchain version, or describe an alternative.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create Cargo manifest, toolchain pin, and crate skeleton</name>
  <files>rust-toolchain.toml, Cargo.toml, .gitignore, src/lib.rs, src/error.rs</files>
  <read_first>
    - .planning/phases/01-schema-data-contract/01-RESEARCH.md (Standard Stack: exact versions + sqlx feature set; Recommended Project Structure; Installation block)
    - CLAUDE.md (locked stack + "What NOT to Use")
  </read_first>
  <action>Create `rust-toolchain.toml` pinning `[toolchain] channel = "1.94.0"`. Create `Cargo.toml` for a library crate (package name `web-of-trust`, edition 2021, `rust-version = "1.94"`) with these exact dependencies: `sqlx = { version = "0.9.0", default-features = false, features = ["runtime-tokio", "tls-rustls", "postgres", "macros", "migrate", "chrono"] }`, `tokio = { version = "1.52", features = ["rt-multi-thread", "macros"] }`, `thiserror = "2.0.18"`, `anyhow = "1.0.102"`, `config = "0.15.23"`; and dev-dependencies `testcontainers = "0.27.3"`, `testcontainers-modules = { version = "0.15.0", features = ["postgres"] }`. Add `.gitignore` ignoring `/target` (do NOT ignore `.sqlx/` — it is committed offline metadata per Pitfall 2). Create `src/lib.rs` declaring `pub mod error;` and a `pub mod store;` placeholder module (an empty `src/store/mod.rs` stub so the crate compiles; Plan 03 fills it). Create `src/error.rs` defining `pub enum StoreError` deriving `thiserror::Error` with at least a `#[error(transparent)] Sqlx(#[from] sqlx::Error)` variant and a `#[error("migration failed: {0}")] Migrate(#[from] sqlx::migrate::MigrateError)` variant. Do not enable the `uuid` sqlx feature (pubkeys are bytea, ids are bigint). Do not add diesel or sea-orm.</action>
  <verify>
    <automated>cargo build 2>&1 | tail -5; cargo build</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0
    - `Cargo.toml` contains `rust-version = "1.94"` and the sqlx feature list `["runtime-tokio", "tls-rustls", "postgres", "macros", "migrate", "chrono"]`
    - `Cargo.toml` does NOT contain `diesel`, `sea-orm`, or the sqlx `uuid` feature
    - `.gitignore` contains `/target` and does NOT contain `.sqlx`
    - `src/error.rs` defines `StoreError` with `#[from] sqlx::Error` and `#[from] sqlx::migrate::MigrateError`
  </acceptance_criteria>
  <done>The crate compiles cleanly with the locked dependency set on the pinned toolchain; StoreError exists; target is gitignored and .sqlx is not.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shared testcontainers Postgres bootstrap fixture</name>
  <files>tests/common/mod.rs</files>
  <behavior>
    - Calling the bootstrap helper starts an ephemeral Postgres container and returns a started container handle plus a `postgres://postgres:postgres@127.0.0.1:{mapped_port}/postgres` connection URL.
    - A trivial integration test that connects to the returned URL with sqlx and runs `SELECT 1` succeeds.
  </behavior>
  <read_first>
    - .planning/phases/01-schema-data-contract/01-RESEARCH.md (Code Examples: concurrent reader+writer test; Validation Architecture — "Shared test fixture: testcontainers Postgres bootstrap")
    - Cargo.toml (confirm testcontainers dev-deps available)
  </read_first>
  <action>Create `tests/common/mod.rs` exposing an async helper (e.g. `pub async fn start_postgres() -> anyhow::Result<(testcontainers::ContainerAsync<testcontainers_modules::postgres::Postgres>, String)>`) that calls `Postgres::default().start().await?`, reads `get_host_port_ipv4(5432).await?`, and formats the connection URL `postgres://postgres:postgres@127.0.0.1:{port}/postgres`. Return both the container handle (caller keeps it alive) and the URL string. Add one smoke `#[tokio::test]` in a sibling test file (e.g. `tests/bootstrap.rs` declaring `mod common;`) that starts Postgres, connects with `sqlx::PgPool::connect`, runs `SELECT 1`, and asserts the row. This proves the fixture every later plan reuses works against real Postgres via Docker.</action>
  <verify>
    <automated>cargo test --test bootstrap -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `tests/common/mod.rs` calls `Postgres::default()` and `get_host_port_ipv4(5432)`
    - The helper returns the container handle AND a `postgres://` URL string
    - `cargo test --test bootstrap` passes (requires Docker), proving connect + `SELECT 1`
  </acceptance_criteria>
  <done>An ephemeral Postgres can be started from a test and connected to; the reusable fixture compiles and the smoke test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator system → toolchain install | rustup install modifies the operator's machine; gated by a blocking checkpoint |
| build → crates.io | dependency resolution pulls third-party crates (all audited OK in RESEARCH Package Legitimacy Audit) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | crates.io dependency supply chain | mitigate | All 7 crates verdict OK in RESEARCH Package Legitimacy Audit; versions pinned exactly in Cargo.toml; no `[SLOP]`/`[SUS]` packages present. |
| T-01-02 | Elevation of Privilege | rustup system install | mitigate | Toolchain install gated behind blocking-decision checkpoint (Task 1); operator approves the exact command. |
| T-01-SC | Tampering | cargo/crates installs | mitigate | Package Legitimacy Audit present in RESEARCH (all OK); pinned versions; no auto-approve of unaudited packages. |
</threat_model>

<verification>
- `cargo build` succeeds on toolchain >= 1.94.
- `cargo test --test bootstrap` starts ephemeral Postgres and runs `SELECT 1`.
- `Cargo.toml` dependency set and feature flags match RESEARCH exactly; no diesel/sea-orm/uuid.
</verification>

<success_criteria>
- A compiling Rust library crate exists with pinned toolchain >= 1.94 and the locked dependency set.
- `StoreError` typed error and `error`/`store` module skeleton exist.
- The shared testcontainers Postgres bootstrap fixture works against real Postgres (smoke test green).
</success_criteria>

<output>
Create `.planning/phases/01-schema-data-contract/01-01-SUMMARY.md` when done. Record the resolved toolchain version, the final dependency versions, and the bootstrap helper signature for Plans 02 and 03.
</output>
