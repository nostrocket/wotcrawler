---
phase: 02-relay-acquisition-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/lib.rs
  - src/error.rs
  - src/relay/mod.rs
  - src/relay/nip11.rs
  - src/relay/rate_limit.rs
  - src/relay/fetch.rs
  - src/ingest/mod.rs
  - src/ingest/verify.rs
  - src/ingest/replaceable.rs
  - src/ingest/follow_list.rs
  - tests/common/mod.rs
  - .planning/phases/02-relay-acquisition-validation/02-SPIKES.md
autonomous: true
requirements: [RELAY-01, RELAY-02]
must_haves:
  truths:
    - "cargo build succeeds with nostr-sdk 0.44, governor 0.10, metrics 0.24 on toolchain 1.94"
    - "The relay and ingest modules are registered and the crate compiles with stub function bodies"
    - "A test helper produces a validly signed Event and a forged (invalid-sig) Event with known keys"
    - "The exact nostr-sdk 0.44 accessor for the NIP-11 RelayInformationDocument is recorded (or a reqwest fallback is decided)"
    - "Whether nostr-sdk reconnect provides exponential backoff with jitter is recorded, with a decision on app-side backoff"
  artifacts:
    - path: "Cargo.toml"
      provides: "nostr-sdk, governor, metrics, futures dependencies"
      contains: "nostr-sdk"
    - path: "src/ingest/mod.rs"
      provides: "ingest module orchestrator + ValidatedFollowList contract type"
      contains: "ValidatedFollowList"
    - path: "src/relay/mod.rs"
      provides: "relay module root"
      min_lines: 5
    - path: "src/error.rs"
      provides: "RelayError + IngestError typed enums"
      contains: "RelayError"
    - path: "tests/common/mod.rs"
      provides: "signed/forged Event fixtures"
      contains: "fn forged_event"
    - path: ".planning/phases/02-relay-acquisition-validation/02-SPIKES.md"
      provides: "recorded answers to RELAY-01 jitter + RELAY-02 NIP-11 accessor open questions"
      contains: "RELAY-01"
  key_links:
    - from: "src/lib.rs"
      to: "src/relay/mod.rs and src/ingest/mod.rs"
      via: "pub mod declarations"
      pattern: "pub mod (relay|ingest)"
    - from: "src/ingest/mod.rs"
      to: "store::follows::apply_follow_list arg shape"
      via: "ValidatedFollowList fields (follower_pubkey, event_id, created_at, followee_pubkeys)"
      pattern: "followee_pubkeys"
---

<objective>
Lay the Phase 2 foundation: add the locked acquisition/validation dependencies, register the `relay` and `ingest` modules with compiling stub bodies, define the two shared error enums and the `ValidatedFollowList` output-contract type, add the signed/forged event test fixtures every later test depends on, and resolve the two API-verification spikes (RELAY-01 reconnect jitter, RELAY-02 NIP-11 accessor) that gate requirement satisfaction.

Purpose: Waves 2's ingest and relay plans must run in parallel without fighting over `Cargo.toml`, `src/lib.rs`, `src/error.rs`, or `tests/common/mod.rs`. This plan owns all of those shared files and the module skeleton so the later plans only fill in disjoint module bodies. The spikes remove the two MEDIUM-risk unknowns the researcher flagged before any code claims RELAY-01/RELAY-02 satisfied.

Output: A compiling crate with empty/stub `relay/*` and `ingest/*` modules, both error enums, the `ValidatedFollowList` type, event fixtures, and `02-SPIKES.md` recording the two answers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-relay-acquisition-validation/02-RESEARCH.md
@.planning/phases/02-relay-acquisition-validation/02-PATTERNS.md
@.planning/phases/02-relay-acquisition-validation/02-VALIDATION.md
@CLAUDE.md
@Cargo.toml
@src/lib.rs
@src/error.rs
@src/store/mod.rs
@tests/common/mod.rs
@tests/edge_diff.rs
</context>

<artifacts_this_phase_produces>
This plan creates the following new symbols (exclude from drift verification — they do not exist before this phase):

- New crate dependencies: `nostr-sdk` (0.44), `governor` (0.10), `metrics` (0.24), `futures` (dev or main as needed)
- `src/relay/mod.rs`, `src/relay/nip11.rs`, `src/relay/rate_limit.rs`, `src/relay/fetch.rs` (module files; stub bodies in this plan)
- `src/ingest/mod.rs`, `src/ingest/verify.rs`, `src/ingest/replaceable.rs`, `src/ingest/follow_list.rs` (module files; stub bodies in this plan)
- `RelayError` enum (in `src/error.rs`) and its variants
- `IngestError` enum (in `src/error.rs`) and its variants
- `ValidatedFollowList` struct (in `src/ingest/mod.rs`) with fields `follower_pubkey`, `event_id`, `created_at`, `followee_pubkeys`
- `pub mod relay;` and `pub mod ingest;` in `src/lib.rs`; extended `pub use` re-export
- Test fixture functions in `tests/common/mod.rs`: a keypair helper, a signed-event builder, a `forged_event` helper, and same-`created_at` variant helpers
- `.planning/phases/02-relay-acquisition-validation/02-SPIKES.md`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies, register modules, define errors + ValidatedFollowList contract</name>
  <files>Cargo.toml, src/lib.rs, src/error.rs, src/ingest/mod.rs, src/ingest/verify.rs, src/ingest/replaceable.rs, src/ingest/follow_list.rs, src/relay/mod.rs, src/relay/nip11.rs, src/relay/rate_limit.rs, src/relay/fetch.rs</files>
  <read_first>
    - Cargo.toml (current dependency block and feature flags to extend, not replace)
    - src/lib.rs (the flat `pub mod` + re-export convention to extend — PATTERNS lines 34-48)
    - src/error.rs (the `StoreError` thiserror convention to mirror — PATTERNS lines 51-73)
    - src/store/mod.rs (module-orchestrator doc-header + `pub mod` convention to mirror — PATTERNS lines 153-168)
    - src/store/follows.rs (the `apply_follow_list` signature: follower_id, event_id: &[u8], created_at: DateTime<Utc>, followee_ids — the arg shape ValidatedFollowList must be able to feed, PATTERNS lines 105-120)
    - .planning/phases/02-relay-acquisition-validation/02-RESEARCH.md (Standard Stack installation block lines 72-86; Recommended Project Structure lines 153-176)
  </read_first>
  <action>
    Add to `[dependencies]` in Cargo.toml: `nostr-sdk = "0.44"`, `governor = "0.10"`, `metrics = "0.24"`. Add `futures = "0.3"` only if the fetch/stream path needs `StreamExt` (otherwise omit). Do not change existing pinned deps (sqlx 0.9.0, tokio 1.52, thiserror 2.0.18, anyhow, config, chrono).

    In src/lib.rs add `pub mod relay;` and `pub mod ingest;` after `pub mod store;`. Extend the re-export to `pub use error::{StoreError, RelayError, IngestError};`.

    In src/error.rs add two new enums mirroring `StoreError`'s convention (one `#[derive(Debug, Error)]` enum per boundary, `#[error("...")]` per variant, `#[from]` for wrapped library errors, a typed variant carrying the offending value):
    - `RelayError`: a `#[from]` transparent variant wrapping the nostr-sdk client/pool error type; typed variants for relay-not-found, NIP-11 fetch failure, and fetch timeout.
    - `IngestError`: typed variants `InvalidSignature`, `UnsolicitedEvent { wanted: Kind, got: Kind }`, `FutureDated`, `OversizedFollowList(usize)` mirroring the `InvalidPubkey(usize)` value-carrying shape. NOTE: routine validation rejections that are counted via metrics (bad sig, unsolicited, future-dated, oversized in the count-and-skip path) return `false` + a counter — `IngestError` is reserved for genuine failures; document this in the enum doc comment.

    Create src/ingest/mod.rs with a `//!` doc header naming the delegation split (nostr-sdk owns verify/parse/dedup; this module owns the gate orchestration: verify -> kind/author match -> dedup seen-set -> resolve -> extract). Declare `pub mod verify; pub mod replaceable; pub mod follow_list;`. Define the phase output-contract struct `ValidatedFollowList` with fields: `follower_pubkey` (nostr `PublicKey`), `event_id` (the winning event id as bytes or `EventId`), `created_at` (a `chrono::DateTime<Utc>` or nostr `Timestamp` — choose one and document the conversion to the `DateTime<Utc>` that `apply_follow_list` requires), and `followee_pubkeys` (`Vec<PublicKey>`). Add a stub orchestrator function signature returning `ValidatedFollowList` that `todo!()`s or returns a placeholder so the crate compiles.

    Create src/ingest/verify.rs, src/ingest/replaceable.rs, src/ingest/follow_list.rs as stubs: doc headers + the public function signatures named in RESEARCH (an `accept`/verify-gate fn, a `pick_winner` resolver fn, a followee-extraction fn) with `todo!()` bodies. These are filled in plan 02.

    Create src/relay/mod.rs with the module-orchestrator doc header + `pub mod nip11; pub mod rate_limit; pub mod fetch;`. Create src/relay/nip11.rs, src/relay/rate_limit.rs, src/relay/fetch.rs as stubs with doc headers and public function signatures (`todo!()` bodies). These are filled in plan 03.

    Do NOT inline full implementations — stub bodies only. The crate must compile.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -5; cargo build --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0 (toolchain 1.94 compiles nostr-sdk 0.44 — confirms RESEARCH A1).
    - `grep -v '^#' Cargo.toml | grep -c 'nostr-sdk'` returns 1.
    - `grep -c 'pub mod relay' src/lib.rs` returns 1 and `grep -c 'pub mod ingest' src/lib.rs` returns 1.
    - `grep -c 'enum RelayError' src/error.rs` returns 1 and `grep -c 'enum IngestError' src/error.rs` returns 1.
    - `grep -c 'struct ValidatedFollowList' src/ingest/mod.rs` returns 1, and the struct text contains `followee_pubkeys`, `follower_pubkey`, `event_id`, and `created_at`.
    - Files src/relay/{mod,nip11,rate_limit,fetch}.rs and src/ingest/{mod,verify,replaceable,follow_list}.rs all exist.
  </acceptance_criteria>
  <done>The crate compiles with all Phase 2 modules registered, both error enums defined, and the ValidatedFollowList contract type in place; later plans fill disjoint module bodies without touching Cargo.toml/lib.rs/error.rs.</done>
</task>

<task type="auto">
  <name>Task 2: Add signed/forged event fixtures to tests/common</name>
  <files>tests/common/mod.rs</files>
  <read_first>
    - tests/common/mod.rs (existing `start_postgres` fixture style to extend — PATTERNS lines 188-212)
    - tests/edge_diff.rs (the deterministic-seed fixture idiom `fn pk(seed: u8)` and `#[tokio::test] -> anyhow::Result<()>` test style — PATTERNS lines 193-210)
    - .planning/phases/02-relay-acquisition-validation/02-RESEARCH.md (signature-verification gate example lines 365-379; nostr Keys/Event API notes lines 311-420; Assumptions A2-A4 lines 437-445)
    - src/ingest/mod.rs (the ValidatedFollowList type the fixtures will exercise, created in Task 1)
  </read_first>
  <action>
    Extend tests/common/mod.rs (keep `start_postgres` unchanged) with reusable nostr fixtures used by every ingest test in plan 02:
    - A deterministic keypair helper that builds a nostr `Keys` from a fixed seed byte (analogous to `pk(seed)`), so tests are reproducible.
    - A signed-event builder: given `Keys`, a `Kind` (kind 3 ContactList or kind 10002 RelayList), a `created_at` `Timestamp`, and a set of p-tag pubkeys, build and sign a valid `Event` (use nostr's `EventBuilder` / signing API; confirm the exact 0.44 builder name during implementation). Returns a fully-signed `Event` whose `verify()` succeeds.
    - A `forged_event` helper that produces an `Event` whose signature is invalid for its id/pubkey (e.g. build a valid event then mutate content/tags so the stored signature no longer matches, or sign with one key and swap the pubkey) — `verify()` MUST return `Err`.
    - Helpers to produce two events with the SAME `created_at` but different ids (for the tie-break test) and a future-dated event (created_at far beyond now) for the clamp test.
    Keep all helpers `pub` so `tests/*.rs` can import them via `mod common;`. Never `unwrap()` on relay-shaped data in production paths, but test fixtures may `expect()` on construction.
  </action>
  <verify>
    <automated>cargo test --test edge_diff -- --list 2>&1 | tail -3; cargo build --tests --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --tests` exits 0 (fixtures compile against nostr-sdk 0.44).
    - `grep -c 'fn forged_event' tests/common/mod.rs` returns 1.
    - tests/common/mod.rs contains a signed-event builder and a same-`created_at` variant helper (grep for the function names confirms presence).
    - The existing `start_postgres` function is still present (`grep -c 'fn start_postgres' tests/common/mod.rs` returns 1).
  </acceptance_criteria>
  <done>Every later ingest test can obtain a validly-signed event, a forged event, same-timestamp variants, and a future-dated event from tests/common, with no network and no Postgres.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve RELAY-01 (reconnect jitter) and RELAY-02 (NIP-11 accessor) spikes</name>
  <files>.planning/phases/02-relay-acquisition-validation/02-SPIKES.md</files>
  <read_first>
    - .planning/phases/02-relay-acquisition-validation/02-RESEARCH.md (Open Questions 1 and 2 lines 449-457; Pitfall 8 lines 297-301; Pattern 1 lines 178-184; RELAY-02 row line 34; Assumptions A4/A5 lines 442-443)
    - CLAUDE.md (locked stack: nostr-sdk 0.44.1, nostr-relay-pool 0.44.1; "do not hand-roll secp256k1/websocket")
    - Cargo.lock (the exact resolved nostr-sdk / nostr-relay-pool / nostr versions in the build)
  </read_first>
  <action>
    Resolve the two MEDIUM-risk open questions the researcher flagged as first-task spikes, and write findings to a new file 02-SPIKES.md. Use Context7 MCP for nostr-sdk / nostr-relay-pool 0.44 docs (or `ctx7 library nostr-relay-pool` then `ctx7 docs <id> "RelayOptions reconnect retry_interval adjust_retry_interval jitter"`); cross-check against the resolved source in Cargo.lock and docs.rs.

    SPIKE 1 (RELAY-01): Determine whether nostr-sdk's reconnect (`RelayOptions::reconnect=true`, `retry_interval`, `adjust_retry_interval=true`) yields EXPONENTIAL backoff WITH jitter, or only fixed/success-tuned retry. Read the nostr-relay-pool 0.44 reconnect implementation/changelog. Record the exact algorithm. DECISION: if it does not provide exponential+jitter, the spike must specify the app-side wrapper plan 03 will implement (a capped-exponential delay with random jitter layered around reconnect, or per-relay `retry_interval` jitter) — name the concrete approach. Do NOT mark RELAY-01 satisfiable on the default alone.

    SPIKE 2 (RELAY-02): Find the exact nostr-sdk 0.44 accessor that yields a relay's NIP-11 `RelayInformationDocument` (e.g. a `Relay`/`Client` method, or none). Record the exact method signature and path. DECISION: if no SDK accessor exists, specify the fallback — a tiny `reqwest` GET with header `Accept: application/nostr+json` parsed into the `RelayInformationDocument` type — and note that `reqwest` must then be added to Cargo.toml in plan 03. Record which `limitation` fields (`max_limit`, `max_subscriptions`, `max_filters`) are read and the sane default for each when a relay omits them.

    Write 02-SPIKES.md with two sections (`## RELAY-01: reconnect backoff/jitter` and `## RELAY-02: NIP-11 accessor`), each containing: the finding, the cited source (Context7/docs.rs/source line), and the explicit DECISION plan 03 must implement.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-relay-acquisition-validation/02-SPIKES.md && grep -c 'RELAY-01' .planning/phases/02-relay-acquisition-validation/02-SPIKES.md && grep -c 'RELAY-02' .planning/phases/02-relay-acquisition-validation/02-SPIKES.md</automated>
  </verify>
  <acceptance_criteria>
    - 02-SPIKES.md exists.
    - It contains a `## RELAY-01` section stating whether reconnect is exponential+jitter and an explicit DECISION (use SDK default vs. add app-side backoff wrapper, naming the approach).
    - It contains a `## RELAY-02` section stating the exact NIP-11 accessor (or the reqwest fallback decision) and the `max_limit` default.
    - Each section cites a concrete source (Context7 id, docs.rs URL, or Cargo.lock version line).
  </acceptance_criteria>
  <done>Plan 03 can implement RELAY-01 and RELAY-02 against recorded, sourced decisions instead of assumptions — no open API questions remain before the transport code is written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build toolchain → crate | New crate dependencies (nostr-sdk, governor, metrics) are pulled from crates.io; supply-chain trust crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | crates.io installs (nostr-sdk, governor, metrics) | mitigate | All three packages carry `OK` legitimacy verdicts in 02-RESEARCH Package Legitimacy Audit (verified via `gsd-tools query package-legitimacy check --ecosystem crates`); crates have no npm-style postinstall vector; versions pinned to the CLAUDE.md-locked stack. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages → no human checkpoint required. |
| T-02-01 | Information disclosure | error enums | accept | RelayError/IngestError messages must not embed secrets; this phase has no secrets (read-only anonymous client). Low risk. |
</threat_model>

<verification>
- `cargo build` succeeds (RESEARCH A1 confirmed: nostr-sdk 0.44 compiles on toolchain 1.94).
- `cargo build --tests` succeeds (fixtures compile).
- `cargo test` still green on the inherited Phase 1 suite (no regressions in store tests).
- 02-SPIKES.md records sourced decisions for RELAY-01 and RELAY-02.
</verification>

<success_criteria>
- Crate compiles with relay + ingest modules registered, both error enums, and the ValidatedFollowList contract type.
- Signed/forged/same-timestamp/future-dated event fixtures available to all later tests.
- The two API-verification spikes are resolved and recorded with sources and explicit implementation decisions for plan 03.
- No Phase 1 test regressions.
</success_criteria>

<output>
Create `.planning/phases/02-relay-acquisition-validation/02-01-SUMMARY.md` when done.
</output>
