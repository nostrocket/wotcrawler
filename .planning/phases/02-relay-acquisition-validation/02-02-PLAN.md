---
phase: 02-relay-acquisition-validation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/ingest/verify.rs
  - src/ingest/replaceable.rs
  - src/ingest/follow_list.rs
  - src/ingest/mod.rs
  - tests/verify_gate.rs
  - tests/dedup.rs
  - tests/replaceable.rs
  - tests/follow_list_bounds.rs
  - tests/relay_list.rs
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03, INGEST-04, INGEST-05]
must_haves:
  truths:
    - "A forged or invalid-signature event is rejected before acceptance and counted"
    - "An event of the wrong kind or from an unrequested author is dropped and counted"
    - "The same event id arriving from multiple relays is processed at most once"
    - "A future-dated event beyond the configurable clamp is rejected; the newest valid event wins; same-timestamp ties break to the lowest event id"
    - "Malformed p-tags are skipped and an oversized follow list is bounded by a configurable cap without panicking"
    - "kind:10002 (NIP-65) events are resolved under the identical replaceable-event rules"
    - "Only a deduplicated, self-drop-filtered followee set crosses into ValidatedFollowList"
  artifacts:
    - path: "src/ingest/verify.rs"
      provides: "signature-verification + kind/author gate (INGEST-01)"
      contains: "verify"
    - path: "src/ingest/replaceable.rs"
      provides: "clamp + newest-wins + lowest-id tie-break resolver (INGEST-03, INGEST-05)"
      contains: "created_at"
    - path: "src/ingest/follow_list.rs"
      provides: "p-tag extraction, dedup, self-drop, configurable cap (INGEST-04)"
      contains: "public_keys"
    - path: "src/ingest/mod.rs"
      provides: "gate orchestrator emitting ValidatedFollowList + cross-call seen-set (INGEST-02)"
      contains: "ValidatedFollowList"
    - path: "tests/verify_gate.rs"
      provides: "INGEST-01 tests (forged rejected; unsolicited dropped)"
      contains: "unsolicited"
    - path: "tests/replaceable.rs"
      provides: "INGEST-03 tests (future_clamp, tie_break)"
      contains: "tie_break"
  key_links:
    - from: "src/ingest/mod.rs"
      to: "src/ingest/verify.rs, replaceable.rs, follow_list.rs"
      via: "pipeline: verify -> resolve -> extract"
      pattern: "(verify|pick_winner|public_keys)"
    - from: "src/ingest/verify.rs"
      to: "metrics counters"
      via: "metrics::counter! on each reject path"
      pattern: "metrics::counter!"
    - from: "src/ingest/replaceable.rs"
      to: "ValidatedFollowList.event_id / created_at"
      via: "winner's id+created_at feed apply_follow_list short-circuit"
      pattern: "created_at"
---

<objective>
Implement the validation/ingest half of Phase 2: the adversarial-input gate that turns raw relay events into a `ValidatedFollowList` carrying only the deduplicated, self-drop-filtered followee set of the newest valid kind-3 per pubkey. This is where all the correctness risk lives (RESEARCH: the three custom policies are the verify gate, the replaceable-event resolution, and the bounds guards) and it is fully testable offline with no network and no Postgres.

Purpose: Deliver "only correct, deduplicated, newest-wins follow lists emerge" — INGEST-01 (verify before accept + count), INGEST-02 (dedup ids), INGEST-03 (clamp + newest-wins + lowest-id tie-break), INGEST-04 (skip malformed p-tags + bound oversized lists), INGEST-05 (same rules for kind:10002).

Output: Filled `src/ingest/{verify,replaceable,follow_list,mod}.rs` and the five offline ingest test files green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-relay-acquisition-validation/02-RESEARCH.md
@.planning/phases/02-relay-acquisition-validation/02-PATTERNS.md
@.planning/phases/02-relay-acquisition-validation/02-VALIDATION.md
@CLAUDE.md
@src/error.rs
@src/ingest/mod.rs
@src/store/follows.rs
@src/store/pubkeys.rs
@tests/common/mod.rs
@tests/edge_diff.rs
</context>

<artifacts_this_phase_produces>
New symbols created by this plan (exclude from drift verification):

- `src/ingest/verify.rs`: a verify-gate function (`accept`-style) returning bool; metric names `ingest_invalid_signature`, `ingest_unsolicited`
- `src/ingest/replaceable.rs`: a `pick_winner`-style resolver function over `&Event` with a configurable `future_clamp` parameter; metric name for future-dated rejects
- `src/ingest/follow_list.rs`: a followee-extraction function applying dedup + self-drop + configurable `follow_cap`; metric name `ingest_oversized_follow_list`
- `src/ingest/mod.rs`: the orchestrator function consuming raw events + a requested-author set and emitting `ValidatedFollowList`; a cross-call `HashSet<EventId>` seen-set; a config struct or params carrying `future_clamp_secs` and `follow_cap`
- Test files: `tests/verify_gate.rs`, `tests/dedup.rs`, `tests/replaceable.rs`, `tests/follow_list_bounds.rs`, `tests/relay_list.rs`
- Note: `ValidatedFollowList`, `IngestError`, and the `tests/common` fixtures are produced by plan 02-01, not here.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Signature/kind/author verify gate + dedup seen-set (INGEST-01, INGEST-02)</name>
  <files>src/ingest/verify.rs, src/ingest/mod.rs, tests/verify_gate.rs, tests/dedup.rs</files>
  <behavior>
    - A validly-signed event of the requested kind from a requested author is accepted (gate returns true).
    - A forged (invalid-sig) event is rejected (gate returns false) and increments `ingest_invalid_signature`.
    - An event of the wrong kind, or from an author not in the requested set, is rejected (returns false) and increments `ingest_unsolicited`.
    - The same event id presented twice (simulating two relays) is processed at most once: the second occurrence is skipped by the orchestrator's `HashSet<EventId>` seen-set.
  </behavior>
  <read_first>
    - src/ingest/verify.rs (the stub signature from plan 02-01 to fill)
    - src/ingest/mod.rs (the orchestrator stub + ValidatedFollowList + where the seen-set lives — PATTERNS lines 153-168)
    - src/store/pubkeys.rs (the reject-at-boundary idiom, lines 30-33 — PATTERNS lines 124-149)
    - src/store/follows.rs (the HashSet dedup idiom lines 73-78 reused for the seen-set — PATTERNS lines 221-228)
    - .planning/phases/02-relay-acquisition-validation/02-RESEARCH.md (Pattern 4 lines 203-209; signature-gate example lines 365-379; Pitfall 4/5/7 lines 273-295)
    - tests/common/mod.rs (signed/forged event fixtures from plan 02-01)
    - tests/edge_diff.rs (test style: `#[tokio::test] -> anyhow::Result<()>`, `matches!` for typed rejection)
  </read_first>
  <action>
    Fill src/ingest/verify.rs: implement the gate fn `(event: &Event, want_kind: Kind, requested: &HashSet<PublicKey>) -> bool`. Step 1: call `event.verify()` (id + secp256k1 sig — RESEARCH Pattern 4: use `verify()`, NOT `verify_signature()` alone, so a relay returning an event whose id ≠ content is caught); on `Err`, `metrics::counter!("ingest_invalid_signature").increment(1)` and return false. Step 2: if `event.kind != want_kind` OR `!requested.contains(&event.pubkey)`, `metrics::counter!("ingest_unsolicited").increment(1)` and return false (Pitfall 4 — relays are adversarial; never trust the response shape). Return true otherwise. Never `unwrap()` on event fields (V5/V7). Confirm the exact `Event::verify` return type and `Kind` comparison against nostr 0.44 during implementation.

    In src/ingest/mod.rs: implement the orchestrator's cross-call dedup using a `HashSet<EventId>` seen-set (same HashSet idiom as follows.rs line 74). Before running the gate on an event, `if !seen.insert(event.id) { skip }` so a duplicate id from any relay is processed at most once (INGEST-02). Note `fetch_events` already dedupes within one call (RESEARCH INGEST-02); the seen-set covers the cross-call/multi-relay path. Wire the gate into the pipeline (resolve + extract are added in Tasks 2-3).

    Write tests/verify_gate.rs: a test that a signed event passes the gate; a `#[test] fn ...` confirming a `forged_event()` is rejected and (assert the gate returns false). Add a submodule or test named `unsolicited` (so `cargo test verify_gate::unsolicited` resolves per the validation map) asserting wrong-kind and wrong-author events are dropped. Write tests/dedup.rs: present the same event id twice through the orchestrator/seen-set and assert it is counted/processed once.
  </action>
  <verify>
    <automated>cargo test --test verify_gate && cargo test --test dedup</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test verify_gate` passes, including a test path matching `verify_gate::unsolicited` (or a test function named to satisfy `cargo test verify_gate::unsolicited`).
    - `cargo test --test dedup` passes.
    - `grep -c 'verify()' src/ingest/verify.rs` >= 1 (uses id+sig verify, not signature-only).
    - `grep -c 'ingest_invalid_signature' src/ingest/verify.rs` returns 1 and `grep -c 'ingest_unsolicited' src/ingest/verify.rs` returns 1.
    - `grep -c 'HashSet' src/ingest/mod.rs` >= 1 (seen-set present).
    - No `unwrap()` on `event.` fields in src/ingest/verify.rs (`grep -c 'event.*unwrap()' src/ingest/verify.rs` returns 0).
  </acceptance_criteria>
  <done>Forged and unsolicited events are rejected-and-counted at the gate, and duplicate event ids are processed at most once across relays.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replaceable-event resolution — clamp + newest-wins + tie-break (INGEST-03, INGEST-05)</name>
  <files>src/ingest/replaceable.rs, src/ingest/mod.rs, tests/replaceable.rs, tests/relay_list.rs</files>
  <behavior>
    - Among valid events, the one with the highest `created_at` is selected as the winner.
    - An event whose `created_at` exceeds `now + future_clamp_secs` is rejected and never wins (a future-dated junk event cannot pin the list).
    - When two events share the same `created_at`, the one with the LOWEST event id wins (deterministic NIP-01 tie-break; no flapping).
    - The identical resolver applied to kind:10002 (RelayList) events selects the newest valid relay-list per pubkey.
  </behavior>
  <read_first>
    - src/ingest/replaceable.rs (the `pick_winner` stub from plan 02-01)
    - .planning/phases/02-relay-acquisition-validation/02-RESEARCH.md (Pattern 5 lines 211-218; resolver code example lines 381-398; Pitfall 2 lines 261-266; Pitfall 3 lines 267-272; INGEST-05 row line 41)
    - src/store/follows.rs (lines 80-91 — the "compute in Rust, testable without a DB" diff idiom this mirrors; the `applied_event_id`/`applied_created_at` short-circuit the winner must feed, lines 47-71 — PATTERNS lines 105-120)
    - src/ingest/mod.rs (ValidatedFollowList.event_id/created_at the winner populates)
    - tests/common/mod.rs (same-`created_at` variants + future-dated fixture from plan 02-01)
  </read_first>
  <action>
    Fill src/ingest/replaceable.rs: implement `pick_winner(events: impl Iterator<Item=&Event>, now: Timestamp, future_clamp_secs: u64) -> Option<&Event>`. Filter out any event with `created_at > now + future_clamp_secs` (saturating add — Pitfall 2). Then `max_by` on `created_at` ascending, with `.then_with(|| b.id.cmp(&a.id))` so that on equal `created_at` the LOWEST id wins (Pitfall 3 — confirm `EventId` `Ord` byte order against nostr 0.44; if `EventId` does not impl `Ord` directly, compare `as_bytes()`). Count future-dated rejects via a metric. The clamp value comes from config (`future_clamp_secs`, configurable, default ~3600 = 1h per RESEARCH/Pitfall 2) — accept it as a parameter, do not hardcode.

    The resolver is kind-agnostic: it operates on `&Event` regardless of kind, so the SAME function resolves kind-3 ContactList and kind:10002 RelayList (INGEST-05). In src/ingest/mod.rs, after the gate, group accepted events by `(pubkey, kind)` and call `pick_winner` per group; the winner's `id` and `created_at` populate `ValidatedFollowList.event_id`/`created_at` so the Phase 1 `apply_follow_list` id-equality short-circuit (GRAPH-02) sees the true winning id. Convert the nostr `Timestamp` to `chrono::DateTime<Utc>` for the contract field (document the conversion).

    Write tests/replaceable.rs with test paths matching `replaceable::future_clamp` (future-dated event beyond clamp is excluded — assert the winner is the in-range event, not the future one) and `replaceable::tie_break` (two same-`created_at` events → the lower id wins). Write tests/relay_list.rs: build two signed kind:10002 events for one pubkey with different `created_at` and assert `pick_winner` selects the newest (INGEST-05).
  </action>
  <verify>
    <automated>cargo test --test replaceable && cargo test --test relay_list</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test replaceable` passes, with tests resolvable as `replaceable::future_clamp` and `replaceable::tie_break`.
    - `cargo test --test relay_list` passes.
    - `grep -c 'future_clamp_secs' src/ingest/replaceable.rs` >= 1 (clamp is a parameter, not hardcoded).
    - src/ingest/replaceable.rs contains a `then_with` tie-break on the event id (`grep -c 'then_with' src/ingest/replaceable.rs` >= 1).
    - The resolver function takes `&Event` generically (not a kind-3-only type), proven by tests/relay_list.rs calling it on kind:10002 events.
  </acceptance_criteria>
  <done>The newest valid event per (pubkey, kind) wins, future-dated junk is rejected, same-timestamp ties break deterministically to the lowest id, and the same resolver handles kind:10002.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: p-tag extraction, dedup, self-drop, configurable cap + emit ValidatedFollowList (INGEST-04)</name>
  <files>src/ingest/follow_list.rs, src/ingest/mod.rs, tests/follow_list_bounds.rs</files>
  <behavior>
    - Followee pubkeys are extracted via `Tags::public_keys()`, which skips malformed/non-standard p-tags (no panic on a malformed tag).
    - The follower's own pubkey is dropped (self-follow filtered) and repeated p-tags are deduplicated.
    - A follow list exceeding the configurable `follow_cap` is bounded (default: reject + count, not silent truncation) without panicking.
    - The orchestrator emits a `ValidatedFollowList { follower_pubkey, event_id, created_at, followee_pubkeys }` whose followee set matches the deduped, self-drop-filtered, capped extraction from the winning event.
  </behavior>
  <read_first>
    - src/ingest/follow_list.rs (the extraction stub from plan 02-01)
    - .planning/phases/02-relay-acquisition-validation/02-RESEARCH.md (Pattern 6 lines 220-228; p-tag extraction code example lines 400-417; Pitfall 6 lines 285-289; Open Question 4 reject-vs-truncate lines 464-467)
    - src/store/follows.rs (lines 73-91 — the exact HashSet dedup + self-drop idiom to replicate, PATTERNS lines 77-101)
    - src/ingest/mod.rs (ValidatedFollowList shape; the orchestrator that assembles the final value)
    - .planning/phases/02-relay-acquisition-validation/02-PATTERNS.md (lines 77-101, D-06 relay-hints/petnames discarded; D-08 self-drop)
    - tests/common/mod.rs (signed-event builder to construct events with N p-tags, a self-p-tag, duplicate p-tags)
  </read_first>
  <action>
    Fill src/ingest/follow_list.rs: implement `followee_pubkeys(event: &Event, follow_cap: usize) -> ...`. Use `event.tags.public_keys().copied()` (skips malformed p-tags automatically — INGEST-04 malformed-skip), `.filter(|pk| *pk != event.pubkey)` (self-drop, D-08 defense-in-depth), `.filter(|pk| seen.insert(*pk))` with a `HashSet` (dedup repeated p-tags). Discard relay hints and petnames — only the pubkey set crosses (D-06). Apply `follow_cap`: per RESEARCH Open Question 4, DEFAULT to REJECT + `metrics::counter!("ingest_oversized_follow_list").increment(1)` when the extracted set exceeds `follow_cap` (truncation silently corrupts the graph) — make reject-vs-truncate config-driven but default to reject. Never `unwrap()` on tag contents (Pitfall 6 — no panic on adversarial input). The cap value comes from config (configurable per INGEST-04) — accept as a parameter.

    In src/ingest/mod.rs, complete the orchestrator: gate (Task 1) → group + resolve (Task 2) → for the winning kind-3, extract followees (this task) → assemble `ValidatedFollowList { follower_pubkey: winner.pubkey, event_id, created_at, followee_pubkeys }`. This is the phase output contract handed to Phase 3.

    Write tests/follow_list_bounds.rs with test paths matching `follow_list_bounds::malformed` (an event whose tags include a malformed/non-standard p-tag is extracted without panic and the malformed tag is skipped) and `follow_list_bounds::cap` (an event with more p-tags than `follow_cap` is bounded without panic — assert reject behavior: empty/rejected result + counter, or capped per config). Also assert self-follow is dropped and duplicate p-tags collapse.
  </action>
  <verify>
    <automated>cargo test --test follow_list_bounds</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test follow_list_bounds` passes, with tests resolvable as `follow_list_bounds::malformed` and `follow_list_bounds::cap`.
    - `grep -c 'public_keys' src/ingest/follow_list.rs` >= 1 (uses Tags::public_keys, not manual tag walking).
    - `grep -c 'ingest_oversized_follow_list' src/ingest/follow_list.rs` returns 1.
    - `grep -c 'follow_cap' src/ingest/follow_list.rs` >= 1 (cap is a parameter).
    - No panic path on malformed input: `grep -c 'unwrap()' src/ingest/follow_list.rs` returns 0.
    - The orchestrator assembles `ValidatedFollowList` (`grep -c 'ValidatedFollowList' src/ingest/mod.rs` >= 1) with all four fields populated.
  </acceptance_criteria>
  <done>The winning kind-3 yields a deduplicated, self-drop-filtered, cap-bounded followee set inside a ValidatedFollowList, malformed p-tags are skipped, and oversized lists are rejected-and-counted without crashing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay → ingest gate | Every event is adversary-controlled untrusted input from public relays. This is the project's primary input-validation boundary (ASVS V5). |
| event.created_at → resolver | `created_at` is unauthenticated client-supplied data; an adversary picks it freely. |
| event.tags → follow-list extractor | p-tag count and shape are attacker-controlled (follow-bomb, malformed tags). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Spoofing/Tampering | src/ingest/verify.rs | mitigate | `Event::verify()` (recompute id + verify secp256k1 sig via nostr-sdk — never hand-rolled) before any acceptance; failures dropped + counted (`ingest_invalid_signature`). Task 1. |
| T-02-03 | Tampering | src/ingest/verify.rs | mitigate | Assert `kind == requested && pubkey ∈ requested authors`; drop+count unsolicited events a malicious relay injects (`ingest_unsolicited`). Task 1. |
| T-02-04 | Tampering / DoS-of-updates | src/ingest/replaceable.rs | mitigate | Reject `created_at > now + future_clamp_secs` so a future-dated event cannot permanently pin a pubkey's list. Task 2. |
| T-02-05 | Tampering (flapping) | src/ingest/replaceable.rs | mitigate | Deterministic same-timestamp tie-break to lowest event id prevents oscillation between same-`created_at` variants from different relays. Task 2. |
| T-02-06 | DoS (follow-bomb) | src/ingest/follow_list.rs | mitigate | Configurable `follow_cap`; default reject + count (`ingest_oversized_follow_list`); no unbounded allocation. Task 3. |
| T-02-07 | DoS (malformed tag panic) | src/ingest/follow_list.rs | mitigate | `Tags::public_keys()` skips malformed p-tags; no `unwrap()` on tag fields → no panic from adversarial input. Task 3. |
| T-02-08 | DoS (duplicate flood) | src/ingest/mod.rs | mitigate | Cross-call `HashSet<EventId>` seen-set processes each id at most once. Task 1. |
</threat_model>

<verification>
- `cargo test --lib` green (pure validation logic, offline, ~30s).
- `cargo test --test verify_gate --test dedup --test replaceable --test relay_list --test follow_list_bounds` all green.
- Per-task verification map (02-VALIDATION) commands all resolve: `verify_gate`, `verify_gate::unsolicited`, `dedup`, `replaceable::future_clamp`, `replaceable::tie_break`, `follow_list_bounds::malformed`, `follow_list_bounds::cap`, `relay_list`.
- No `unwrap()` on relay-supplied event/tag fields anywhere in `src/ingest/`.
</verification>

<success_criteria>
- INGEST-01: forged/invalid-sig rejected + counted; wrong kind/author dropped + counted.
- INGEST-02: duplicate event ids processed at most once.
- INGEST-03: future-dated beyond clamp rejected; newest-wins; same-ts ties → lowest id.
- INGEST-04: malformed p-tags skipped; oversized list bounded (reject+count) without panic.
- INGEST-05: kind:10002 resolved under the identical replaceable rules.
- The orchestrator emits a correct `ValidatedFollowList` ready for the Phase 3 store writer.
</success_criteria>

<output>
Create `.planning/phases/02-relay-acquisition-validation/02-02-SUMMARY.md` when done.
</output>
