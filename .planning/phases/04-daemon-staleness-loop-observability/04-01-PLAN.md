---
phase: 04-daemon-staleness-loop-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - migrations/0003_staleness.sql
  - src/crawl/frontier.rs
  - src/crawl/mod.rs
  - src/lib.rs
  - src/daemon/mod.rs
  - tests/daemon_config.rs
  - tests/staleness.rs
  - tests/daemon_loop.rs
  - tests/observe.rs
  - tests/common/mod.rs
autonomous: true
requirements: [FRESH-02, FRESH-03, OPS-02]
must_haves:
  truths:
    - "The crawler binary `crawler` is declared and the daemon module compiles into the library"
    - "A staleness UPDATE re-enqueues only rows past the TTL, resetting claimed_at and fetch_attempts, and leaves fresh rows untouched"
    - "An in-run reclaim UPDATE resets only in_progress leases older than a threshold, never freshly-claimed live rows"
    - "Migration 0003 adds a last_fetched_at index and re-running migrations is a no-op"
  artifacts:
    - path: "migrations/0003_staleness.sql"
      provides: "Index supporting the staleness scan over last_fetched_at"
      contains: "CREATE INDEX IF NOT EXISTS"
    - path: "src/crawl/frontier.rs"
      provides: "reclaim_stale_by_ttl and reclaim_in_progress_older_than"
      contains: "pub async fn reclaim_stale_by_ttl"
    - path: "src/daemon/mod.rs"
      provides: "daemon module root (stub orchestrator)"
      contains: "pub"
    - path: "Cargo.toml"
      provides: "New dependencies + [[bin]] crawler"
      contains: "[[bin]]"
  key_links:
    - from: "src/lib.rs"
      to: "src/daemon/mod.rs"
      via: "pub mod daemon"
      pattern: "pub mod daemon"
    - from: "src/crawl/frontier.rs::reclaim_stale_by_ttl"
      to: "pubkeys.last_fetched_at"
      via: "UPDATE ... WHERE last_fetched_at < now() - $1::interval"
      pattern: "last_fetched_at <"
---

<objective>
Lay the Phase 4 foundation that every later plan compiles against: add the new dependencies and the `crawler` binary target, ship the index-only migration 0003, add the two new frontier sweep functions (`reclaim_stale_by_ttl` for FRESH-02, `reclaim_in_progress_older_than` for the in-run OPS-02 reclaim), open `join_worker` for reuse, register the `daemon` module, and scaffold the Wave 0 test binaries so later tasks have failing targets to fill.

Purpose: This is the wiring-phase keystone — deps + frontier sweeps + module registration must exist before config, observability, loop, and main can compile and be tested.
Output: A compiling library + binary target, migration 0003, two new frontier functions with green tests, regenerated `.sqlx`, and named-ignored Wave 0 test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-daemon-staleness-loop-observability/04-CONTEXT.md
@.planning/phases/04-daemon-staleness-loop-observability/04-RESEARCH.md
@.planning/phases/04-daemon-staleness-loop-observability/04-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan creates / modifies:
- `Cargo.toml` — adds deps: `clap` 4.6.1 (feature `derive`), `tracing` 0.1.44, `tracing-subscriber` 0.3.23 (features `env-filter`, `json`, `fmt`), `metrics-exporter-prometheus` 0.18.3 (`default-features = false`), `axum` 0.8.9, `tokio-util` 0.7.18 (feature `rt`), `humantime-serde` 1.1.1; bumps `tokio` features to add `signal` and `time`; adds dev-dep `tower` (for `ServiceExt::oneshot`); adds `[[bin]] name = "crawler" path = "src/main.rs"`.
- `migrations/0003_staleness.sql` — index-only: `pubkeys_last_fetched_idx`.
- `src/crawl/frontier.rs` — new `pub async fn reclaim_stale_by_ttl(pool, ttl_secs: i64) -> Result<u64, StoreError>` and `pub async fn reclaim_in_progress_older_than(pool, age_secs: i64) -> Result<u64, StoreError>`.
- `src/crawl/mod.rs` — `join_worker` visibility changed to `pub(crate)`.
- `src/daemon/mod.rs` — new module root (stub `run` orchestrator placeholder; filled in 04-05).
- `src/lib.rs` — `pub mod daemon;`.
- Wave 0 test scaffolds: `tests/daemon_config.rs`, `tests/staleness.rs`, `tests/daemon_loop.rs`, `tests/observe.rs` (named `#[ignore]` stubs filled by later plans), plus `tests/staleness.rs` real tests for the two new frontier sweeps.
- `tests/common/mod.rs` — promote the `ScriptedGraph` mock + `fetch_fn` (currently private to `tests/frontier.rs`) into shared `common` so `tests/daemon_loop.rs` can reuse the injected-`fetch_union` seam.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add deps + binary target + register daemon module</name>
  <files>Cargo.toml, src/lib.rs, src/crawl/mod.rs, src/daemon/mod.rs</files>
  <read_first>
    - Cargo.toml (current deps + dev-deps; no [[bin]] today — crate is library-only)
    - 04-RESEARCH.md §Standard Stack "Installation" block (exact versions + features; `metrics-exporter-prometheus` MUST be `default-features = false` — NO http-listener; do NOT add `tower-http`)
    - src/lib.rs:9-15 (module declaration idiom + re-exports)
    - src/crawl/mod.rs:223-240 (`join_worker` — currently private `async fn`)
    - 04-PATTERNS.md §"Shared Conventions" item 1 (module-doc header citing requirement IDs)
  </read_first>
  <action>
    In Cargo.toml add the verified deps per 04-RESEARCH Installation: `clap = { version = "4.6.1", features = ["derive"] }`, `tracing = "0.1.44"`, `tracing-subscriber = { version = "0.3.23", features = ["env-filter", "json", "fmt"] }`, `metrics-exporter-prometheus = { version = "0.18.3", default-features = false }`, `axum = "0.8.9"`, `tokio-util = { version = "0.7.18", features = ["rt"] }`, `humantime-serde = "1.1.1"`. Change the existing `tokio` line to features `["rt-multi-thread", "macros", "signal", "time"]`. Add dev-dep `tower = "0.5"` (for `ServiceExt::oneshot` in `tests/observe.rs`). Append a `[[bin]]` section: `name = "crawler"`, `path = "src/main.rs"`. Create `src/main.rs` as a minimal placeholder `fn main()` (the real entry lands in 04-05) so the `[[bin]]` target compiles now. Create `src/daemon/mod.rs` with a `//!` module-doc header citing OPS-01/OPS-02/OBS-01..05/FRESH-02 (per 04-PATTERNS Shared Convention 1) and declaring `pub mod` placeholders only as later plans add submodules — for now an empty module body is fine. Add `pub mod daemon;` to src/lib.rs. Change `async fn join_worker` to `pub(crate) async fn join_worker` in src/crawl/mod.rs (RESEARCH Open Question 4 / Pattern 2 note: the daemon loop reuses it). Do NOT add `tower-http`. Do NOT enable the `http-listener` feature.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo build --all-targets 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '\[\[bin\]\]' Cargo.toml` shows the bin section and `grep -n 'name = "crawler"' Cargo.toml` matches.
    - `grep -c 'http-listener' Cargo.toml` returns 0 (feature NOT enabled) and `grep -c 'tower-http' Cargo.toml` returns 0.
    - `grep -n 'signal' Cargo.toml` shows tokio gained the `signal` feature.
    - `grep -n 'pub mod daemon' src/lib.rs` matches.
    - `grep -n 'pub(crate) async fn join_worker' src/crawl/mod.rs` matches.
    - `SQLX_OFFLINE=true cargo build --all-targets` exits 0.
  </acceptance_criteria>
  <done>Workspace compiles with the new deps, the `crawler` bin target, and the registered `daemon` module; `join_worker` is `pub(crate)`.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0003 (index-only) + frontier sweep functions + .sqlx</name>
  <files>migrations/0003_staleness.sql, src/crawl/frontier.rs</files>
  <read_first>
    - migrations/0002_frontier.sql (whole file — additive/idempotent header block + `IF NOT EXISTS` idiom to copy)
    - migrations/0001_graph_schema.sql:40-45 (existing partial `pubkeys_status_idx` — it is `WHERE status IN ('discovered','not_found','failed')`, deliberately EXCLUDING `fetched`, which the staleness scan must re-enqueue — RESEARCH Pitfall 2)
    - src/crawl/frontier.rs:99-113 (`reclaim_stale_on_startup` — the exact UPDATE shape, return type `Result<u64, StoreError>` via `rows_affected()`, and the load-bearing `claimed_at = NULL, fetch_attempts = 0` reset comment at 101-105)
    - src/crawl/frontier.rs:142-160 (`requeue_or_fail` — the `$N::int2`/typed-bind cast idiom for typed binds)
    - 04-RESEARCH.md §Pattern 3 (staleness SQL) + §Open Question 1 (migration 0003 is INDEX-ONLY; churn columns already exist — do NOT add columns) + §Open Question 2 (in-run reclaim age threshold)
    - 04-PATTERNS.md §"migrations/0003_staleness.sql" and §"frontier.rs add reclaim_stale_by_ttl"
  </read_first>
  <action>
    Create `migrations/0003_staleness.sql` following the 0002 header/idempotency conventions: a `--` doc block stating it is strictly additive + idempotent and that it adds NO columns (churn columns `last_changed_at`/`fetch_count`/`change_count` already exist from 0001 and are written by `apply_follow_list`, per RESEARCH A3); then `CREATE INDEX IF NOT EXISTS pubkeys_last_fetched_idx ON pubkeys (last_fetched_at);`. Do NOT add any `ALTER TABLE ... ADD COLUMN` (RESEARCH Open Question 1 / 04-PATTERNS — migration is index-only). In src/crawl/frontier.rs add `pub async fn reclaim_stale_by_ttl(pool: &PgPool, ttl_secs: i64) -> Result<u64, StoreError>`: a single `sqlx::query!` UPDATE on the pool (not in a txn — RESEARCH anti-pattern: do not wrap the sweep in a long txn) of the form `UPDATE pubkeys SET status = 'discovered', claimed_at = NULL, fetch_attempts = 0 WHERE status IN ('fetched','not_found','failed') AND last_fetched_at < now() - make_interval(secs => $1)` binding `ttl_secs`, returning `result.rows_affected()`. Carry forward the load-bearing comment from frontier.rs:101-105 explaining why `claimed_at`/`fetch_attempts` reset (a re-fetch cycle must not inherit prior relay-failure retry counts). Add `pub async fn reclaim_in_progress_older_than(pool: &PgPool, age_secs: i64) -> Result<u64, StoreError>`: same single-statement-on-pool shape, `UPDATE pubkeys SET status = 'discovered', claimed_at = NULL, fetch_attempts = 0 WHERE status = 'in_progress' AND claimed_at < now() - make_interval(secs => $1)` binding `age_secs`, doc-commenting that the age threshold (a few × fetch_timeout) is what prevents resetting freshly-claimed live leases (RESEARCH Open Question 2). Use `make_interval(secs => $1)` so the bind is a plain `i64` (avoids interval-string formatting). Both functions cite FRESH-02 / OPS-02 in their doc-comments per Shared Convention 1. Then regenerate offline metadata: run `cargo sqlx prepare` against a DB with migration 0003 applied, and commit the `.sqlx/` drift.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo build --lib 2>&1 | tail -10 && grep -c '^--\|CREATE INDEX' migrations/0003_staleness.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ADD COLUMN' migrations/0003_staleness.sql` returns 0 (index-only, no columns).
    - `grep -n 'CREATE INDEX IF NOT EXISTS pubkeys_last_fetched_idx' migrations/0003_staleness.sql` matches.
    - `grep -n 'pub async fn reclaim_stale_by_ttl' src/crawl/frontier.rs` and `grep -n 'pub async fn reclaim_in_progress_older_than' src/crawl/frontier.rs` both match.
    - `grep -n 'make_interval' src/crawl/frontier.rs` matches in both new functions; `grep -n "claimed_at = NULL, fetch_attempts = 0" src/crawl/frontier.rs` appears in both.
    - `.sqlx/` contains query files for both new statements (`ls .sqlx/ | wc -l` increased) and `SQLX_OFFLINE=true cargo build --lib` exits 0.
  </acceptance_criteria>
  <done>Migration 0003 is index-only and idempotent; both frontier sweep functions exist with the reset semantics; `.sqlx` regenerated and offline build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 test scaffolds + staleness/reclaim integration tests + shared ScriptedGraph</name>
  <files>tests/staleness.rs, tests/daemon_config.rs, tests/daemon_loop.rs, tests/observe.rs, tests/common/mod.rs</files>
  <behavior>
    - reenqueues_only_stale: seed three `fetched` rows with `last_fetched_at` at now-1h, now-48h, now-72h; `reclaim_stale_by_ttl(pool, 24*3600)` flips ONLY the two past-24h rows to `discovered` and leaves the fresh one `fetched`; returns rows_affected == 2.
    - resets_attempts: a stale row seeded with `fetch_attempts = 2` and `claimed_at = now()` comes back from `reclaim_stale_by_ttl` with `fetch_attempts = 0` and `claimed_at IS NULL`.
    - not_found_and_failed_also_reenqueued: `not_found` and `failed` rows past TTL are also flipped to `discovered` (the scanner covers all three terminal statuses).
    - in_progress_only_reclaims_old: with `reclaim_in_progress_older_than(pool, age)`, an `in_progress` row claimed `age+1`s ago is reset to `discovered`; a freshly-claimed `in_progress` row (claimed_at = now()) is left untouched.
  </behavior>
  <read_first>
    - tests/frontier.rs:42-79 (the `ScriptedGraph` struct + `union_for` + `fetch_fn` closure to PROMOTE into common) and tests/frontier.rs:103-120 (`fresh_db`, `status_of`, `pk` helpers to reuse)
    - tests/frontier.rs:235-285 (`startup_reclaims_in_progress` — the exact seed/assert shape for a reclaim test)
    - tests/common/mod.rs:1-60 (shared-fixture conventions; `start_postgres`, `keys`, `signed_event`; `#![allow(dead_code)]`)
    - 04-RESEARCH.md §Validation Architecture "Phase Requirements → Test Map" (FRESH-02 row) and §Test Seams (inject CancellationToken; build_recorder() not global install; oneshot via tower::ServiceExt)
    - 04-VALIDATION.md §Wave 0 Requirements + §Per-Task Verification Map
  </read_first>
  <action>
    Move `ScriptedGraph` (tests/frontier.rs:42-79) and the `follows_event`/`pk`/`fresh_db`/`status_of` helpers into `tests/common/mod.rs` (under `#![allow(dead_code)]`) so both `tests/frontier.rs` and `tests/daemon_loop.rs` share one mock; update `tests/frontier.rs` to use the promoted versions (re-export or `use common::...`) without changing its existing test bodies. Create `tests/staleness.rs` with the FOUR tests in `<behavior>` as real `#[tokio::test]` integration tests using the promoted `fresh_db` + a `seed_row(pool, pk, status, last_fetched_at, fetch_attempts, claimed_at)` helper that inserts directly via `sqlx::query!`; each test seeds rows, calls the new frontier sweep, and asserts status/attempts/claimed_at and the returned count. Create the OTHER Wave 0 scaffolds as named `#[ignore]` stub tests so later plans fill them: `tests/daemon_config.rs` (`override_precedence`, `default_fill`, `invalid_anchor_rejected`, `ttl_zero_rejected` — all `#[ignore = "filled in 04-02"]`); `tests/daemon_loop.rs` (`graceful_drain_no_orphan_leases`, `idle_then_resume_after_reenqueue` — `#[ignore = "filled in 04-04"]`); `tests/observe.rs` (`metrics_endpoint_exposes_series`, `live_always_ok`, `ready_requires_db_and_loop`, `json_format_selected`, `progress_summary_counts`, `dashboard_json_valid` — `#[ignore = "filled in 04-03/04-04"]`). Every test binary opens with `mod common;`. Run `tests/staleness.rs` with `-- --test-threads=2` (KNOWN testcontainers race).
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo test --test staleness -- --test-threads=2 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `tests/staleness.rs` defines exactly the four named tests in `<behavior>` and they pass (run with `-- --test-threads=2`; re-run once on a container/port race).
    - `grep -n 'struct ScriptedGraph' tests/common/mod.rs` matches (mock promoted) and `tests/frontier.rs` still compiles + its existing tests still pass (`SQLX_OFFLINE=true cargo test --test frontier -- --test-threads=2`).
    - `tests/daemon_config.rs`, `tests/daemon_loop.rs`, `tests/observe.rs` each exist with the named `#[ignore]` stubs (`SQLX_OFFLINE=true cargo test --test daemon_config --test daemon_loop --test observe -- --list` lists them).
    - `SQLX_OFFLINE=true cargo build --all-targets` exits 0.
  </acceptance_criteria>
  <done>Staleness + in-run-reclaim behavior is proven green over a real DB; the shared ScriptedGraph is available to daemon_loop tests; all four Wave 0 scaffolds exist with named ignored stubs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config → daemon | DB URL / anchor / relay set cross into the process (handled in 04-02) |
| Postgres ← sweep UPDATE | the staleness/reclaim UPDATEs mutate frontier state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | staleness scan full-table-scan at millions of rows | mitigate | Migration 0003 adds `pubkeys_last_fetched_idx`; scan keyed on indexed `last_fetched_at` (RESEARCH Pitfall 2) |
| T-04-02 | Tampering | in-run reclaim resetting a live lease mid-fetch | mitigate | `reclaim_in_progress_older_than` keys on `claimed_at < now() - age`, never freshly-claimed rows (RESEARCH Open Question 2) |
| T-04-SC | Tampering | cargo dependency installs | mitigate | All new crates verified in RESEARCH §Package Legitimacy Audit (tokio-rs / clap-rs / metrics-rs first-party, multi-year, tens-of-millions downloads); no [ASSUMED]/[SUS]/[SLOP] |
</threat_model>

<verification>
- `SQLX_OFFLINE=true cargo build --all-targets` exits 0.
- `SQLX_OFFLINE=true cargo test --test staleness -- --test-threads=2` green (re-run once on container race).
- `SQLX_OFFLINE=true cargo test --test frontier -- --test-threads=2` still green (no regression from promoting ScriptedGraph).
- `cargo clippy --all-targets` shows no new warnings on touched files.
- `.sqlx/` regenerated with zero drift (`cargo sqlx prepare --check` clean once a 0003-migrated DB is available).
</verification>

<success_criteria>
- New deps + `crawler` bin target compile; `daemon` module registered.
- Migration 0003 is index-only and idempotent.
- `reclaim_stale_by_ttl` and `reclaim_in_progress_older_than` proven correct by green DB tests.
- Wave 0 scaffolds exist for config, loop, and observe suites.
</success_criteria>

<output>
Create `.planning/phases/04-daemon-staleness-loop-observability/04-01-SUMMARY.md` when done.
</output>
