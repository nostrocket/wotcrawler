---
phase: 03-graph-writer-bfs-frontier
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0002_frontier.sql
  - tests/graph_writer.rs
  - tests/frontier.rs
  - tests/migrations.rs
  - .sqlx/
autonomous: true
requirements: [GRAPH-02, CRAWL-03, FRESH-01]
tags: [postgres, sqlx, migration, frontier, nostr]

must_haves:
  truths:
    - "Running migrations from empty brings the DB to schema 0002 and re-running 0002 is a no-op"
    - "pubkeys.status accepts the new value 'in_progress' and rejects out-of-domain values"
    - "pubkey_freshness never exposes 'in_progress' — it collapses to 'discovered'"
    - "claimed_at and fetch_attempts columns exist and are absent from every contract view"
    - "tests/graph_writer.rs and tests/frontier.rs compile as Wave 0 scaffolds with named, ignored-or-failing stub tests for every Phase 3 requirement"
  artifacts:
    - path: "migrations/0002_frontier.sql"
      provides: "Additive frontier migration: 'in_progress' status, claimed_at, fetch_attempts, pubkey_freshness collapse"
      contains: "pubkeys_status_check"
    - path: "tests/graph_writer.rs"
      provides: "GRAPH-02 verification test scaffold (real ValidatedFollowList through the wired seam)"
    - path: "tests/frontier.rs"
      provides: "Frontier test scaffold: claim/lease, reachability, crash-resume, bounded concurrency, terminal stamping"
  key_links:
    - from: "migrations/0002_frontier.sql"
      to: "pubkeys.status CHECK"
      via: "DROP CONSTRAINT IF EXISTS pubkeys_status_check + ADD CONSTRAINT named"
      pattern: "DROP CONSTRAINT IF EXISTS pubkeys_status_check"
    - from: "tests/migrations.rs"
      to: "0002 re-run no-op"
      via: "sqlx::migrate! run twice, assert _sqlx_migrations count unchanged"
      pattern: "_sqlx_migrations"
---

<objective>
Ship the one additive migration that turns the existing `pubkeys.status` lifecycle
into a crash-safe, DB-resident BFS frontier (D-12), and lay down the Wave 0 test
scaffolds (`tests/graph_writer.rs`, `tests/frontier.rs`) so every Phase 3
requirement has a named test that exists before its implementation lands.

Purpose: The migration is the schema delta every later Phase 3 task builds on
(claim query reads `in_progress`, retry bumps `fetch_attempts`, the lease stamps
`claimed_at`). The Validation Architecture (03-RESEARCH.md) flags both test files
as Wave 0 gaps — they must exist as compiling scaffolds so subsequent plans fill
bodies rather than create files.

Output: `migrations/0002_frontier.sql`, extended `tests/migrations.rs` idempotency
assertion, scaffolded `tests/graph_writer.rs` + `tests/frontier.rs`, regenerated
`.sqlx/` metadata.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-graph-writer-bfs-frontier/03-CONTEXT.md
@.planning/phases/03-graph-writer-bfs-frontier/03-RESEARCH.md
@.planning/phases/03-graph-writer-bfs-frontier/03-PATTERNS.md
@migrations/0001_graph_schema.sql
@tests/migrations.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the live CHECK constraint name, then write the additive frontier migration</name>
  <files>migrations/0002_frontier.sql</files>
  <read_first>
    - migrations/0001_graph_schema.sql (the full Phase 1 schema; lines 17-29 status CHECK + freshness columns, lines 42-45 partial index, lines 55-56 pubkey_freshness view, lines 79-82 status contract COMMENT, lines 84-94 INTERNAL column comments)
    - .planning/phases/03-graph-writer-bfs-frontier/03-RESEARCH.md (Pitfall 1 CHECK-widening, Assumption A1 constraint name, "Additive frontier migration (D-12)" code example, Open Question 2 in_progress exposure)
    - .planning/phases/03-graph-writer-bfs-frontier/03-PATTERNS.md ("migrations/0002_frontier.sql" section, lines 31-72)
  </read_first>
  <action>
    First confirm the auto-generated CHECK constraint name (RESEARCH A1): start a throwaway Postgres via the testcontainers fixture or a local DB, run `migrations/0001_graph_schema.sql`, and inspect the constraint name on `pubkeys.status` (e.g. `psql \d pubkeys` or query `pg_constraint`/`information_schema.check_constraints`). The expected name is `pubkeys_status_check`; if it differs, use the real name in the DROP.
    Create `migrations/0002_frontier.sql` as an idempotent additive migration mirroring the 0001 header doc-block convention (state it is additive + idempotent, name the scope: frontier support per D-12). Implement, in order:
    1. Widen the status CHECK domain (Pitfall 1 — CHECK cannot be edited in place): `ALTER TABLE pubkeys DROP CONSTRAINT IF EXISTS pubkeys_status_check;` then `ALTER TABLE pubkeys ADD CONSTRAINT pubkeys_status_check CHECK (status IN ('discovered','in_progress','fetched','not_found','failed'));` using the explicit named constraint so re-runs are clean.
    2. Add internal bookkeeping columns (D-12): `ALTER TABLE pubkeys ADD COLUMN IF NOT EXISTS claimed_at TIMESTAMPTZ;` and `ALTER TABLE pubkeys ADD COLUMN IF NOT EXISTS fetch_attempts SMALLINT NOT NULL DEFAULT 0;`.
    3. Collapse transient `in_progress` out of the contract (D-12, Open Question 2 recommendation — hide it): `CREATE OR REPLACE VIEW pubkey_freshness AS SELECT id, CASE WHEN status = 'in_progress' THEN 'discovered' ELSE status END AS status, last_fetched_at FROM pubkeys;` so the documented contract domain `{discovered, fetched, not_found, failed}` stays truthful.
    4. `COMMENT ON COLUMN pubkeys.claimed_at` and `pubkeys.fetch_attempts` labelled `INTERNAL: ...` matching the 0001 internal-comment style (D-11/D-12). Re-issue the `pubkey_freshness.status` PUBLIC CONTRACT comment so it still documents the four-value domain after the view redefine.
    Do NOT add an `in_progress` partial index (RESEARCH A3 — claim reads only `discovered`, sweep is a one-time startup scan). Do NOT alter the `follows` table or any other Phase 1 object.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo test --test migrations -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Source: `migrations/0002_frontier.sql` contains `DROP CONSTRAINT IF EXISTS pubkeys_status_check` and `ADD CONSTRAINT pubkeys_status_check CHECK (status IN (...'in_progress'...))`.
    - Source: file contains `ADD COLUMN IF NOT EXISTS claimed_at` and `ADD COLUMN IF NOT EXISTS fetch_attempts SMALLINT NOT NULL DEFAULT 0`.
    - Source: `CREATE OR REPLACE VIEW pubkey_freshness` collapses `in_progress` to `discovered` via a CASE expression.
    - Behavior: migrating an empty DB through 0001+0002 succeeds; an `UPDATE pubkeys SET status='in_progress'` succeeds and `SELECT status FROM pubkey_freshness` for that row returns `discovered`.
    - Behavior: `information_schema.columns` shows `pubkey_freshness` exposes only `id, status, last_fetched_at` (no `claimed_at`/`fetch_attempts`).
  </acceptance_criteria>
  <done>0002 migration exists, is idempotent (re-run no-op), widens the status domain, adds the two internal columns, and hides `in_progress` from `pubkey_freshness`.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the migration idempotency test for 0002</name>
  <files>tests/migrations.rs</files>
  <read_first>
    - tests/migrations.rs (existing `migrations_idempotent` test, lines 16-47; the schema-shape test below it)
    - migrations/0002_frontier.sql (written in Task 1)
    - tests/common/mod.rs (start_postgres fixture, lines around 29-34)
  </read_first>
  <action>
    Add a test (or extend the schema-shape test) named `migration_0002_widens_status_and_hides_in_progress` that: migrates a fresh DB through the full `migrations/` dir, asserts an `UPDATE pubkeys SET status='in_progress'` on a seeded row succeeds (proving the widened CHECK), asserts `SELECT status FROM pubkey_freshness WHERE id = $1` returns `'discovered'` for that in_progress row (proving the collapse), and asserts via `information_schema.columns` that `pubkey_freshness` does NOT expose `claimed_at` or `fetch_attempts`. The existing `migrations_idempotent` test already runs the whole dir twice and asserts the `_sqlx_migrations` count is unchanged, so it now covers 0002 re-run no-op (RESEARCH Pitfall 1) — verify it still passes; do not duplicate that assertion.
    Use a deterministic `pk(seed)`-style 32-byte key inserted via a parameterized `INSERT INTO pubkeys (pubkey) ...` or `upsert_pubkey`. All queries parameterized — never string-formatted SQL.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo test --test migrations</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `migrations_idempotent` passes (0002 re-run is a no-op; `_sqlx_migrations` count unchanged across two runs).
    - Behavior: new test sets a row to `in_progress` successfully and reads it back as `discovered` through `pubkey_freshness`.
    - Source: a test asserts `pubkey_freshness` columns do not include `claimed_at`/`fetch_attempts`.
    - Test command `SQLX_OFFLINE=true cargo test --test migrations` exits 0.
  </acceptance_criteria>
  <done>The migration test suite proves 0002 idempotency, the widened status domain, and the `in_progress` collapse in the contract view.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold tests/graph_writer.rs and tests/frontier.rs (Wave 0 gaps)</name>
  <files>tests/graph_writer.rs, tests/frontier.rs</files>
  <read_first>
    - tests/edge_diff.rs (Postgres-fixture diff test harness, lines 7-11 imports, 18-27 edge_count helper, 31-36 setup, 102-143 zero-touch assertions)
    - tests/concurrency.rs (two-pool concurrent idiom, lines 1-40; pk(seed) helper lines 14-19)
    - tests/acquire_pipeline.rs (building real signed events / ValidatedFollowList)
    - tests/mock_relay/mod.rs (ScriptedRelay + event_at, lines 1-50)
    - .planning/phases/03-graph-writer-bfs-frontier/03-RESEARCH.md (Validation Architecture: Phase Requirements -> Test Map table, lines 488-495; the exact test names per requirement)
  </read_first>
  <action>
    Create both test files as COMPILING Wave 0 scaffolds (`mod common;` + the testcontainers `start_postgres` setup), each containing named stub tests that map 1:1 to the RESEARCH Test Map. Mark every body-less stub `#[ignore]` with a `// Wave 0 scaffold — body lands in 03-03` comment so the suite compiles and the names exist as the contract subsequent plans must satisfy. Do NOT implement bodies here (that is 03-03's job) — the scaffold's value is that the test names exist and compile.
    `tests/graph_writer.rs` stubs (GRAPH-02): `apply_diff_adds_and_removes`, `same_event_zero_touch`, `newest_wins_under_concurrent_apply`.
    `tests/frontier.rs` stubs (CRAWL-01..04, FRESH-01): `bfs_reaches_full_component`, `spam_island_never_crawled`, `crash_resume_no_redo`, `startup_reclaims_in_progress`, `bounded_concurrency`, `skip_locked_no_double_claim`, `last_fetched_at_stamped_on_terminal`.
    Import surfaces that exist today (`web_of_trust::store::{self, follows::apply_follow_list, pubkeys::upsert_pubkey}`); do NOT import `web_of_trust::crawl::*` yet (that module lands in 03-02) — keep the `#[ignore]` stub bodies free of not-yet-existing symbols so the file compiles now. Add a deterministic `pk(seed)` helper mirroring concurrency.rs.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo test --no-run --test graph_writer --test frontier</automated>
  </verify>
  <acceptance_criteria>
    - Source: `tests/graph_writer.rs` defines `apply_diff_adds_and_removes`, `same_event_zero_touch`, `newest_wins_under_concurrent_apply` (all `#[ignore]` stubs).
    - Source: `tests/frontier.rs` defines all seven named stubs from the Test Map (all `#[ignore]`).
    - Behavior: `cargo test --no-run --test graph_writer --test frontier` compiles both suites cleanly (exit 0).
    - Source: neither file references `web_of_trust::crawl` (module does not exist until 03-02).
  </acceptance_criteria>
  <done>Both Wave 0 test files exist, compile, and declare every Phase 3 requirement's test by name as ignored scaffolds.</done>
</task>

<task type="auto">
  <name>Task 4: Regenerate and commit .sqlx offline metadata</name>
  <files>.sqlx/</files>
  <read_first>
    - .planning/phases/03-graph-writer-bfs-frontier/03-RESEARCH.md (Pitfall 2 stale .sqlx metadata; Runtime State Inventory "Build artifacts" row)
    - src/store/mod.rs (run_migrations + the .sqlx offline convention, lines 38-46)
  </read_first>
  <action>
    This plan adds no new `query!`/`query_scalar!` macros in `src/` (migration is raw SQL; tests use untyped `sqlx::query`/`query_scalar`), so the `.sqlx/` set should be unchanged. Run `cargo sqlx prepare -- --all-targets` against a DB migrated through 0002 to confirm no drift, and commit any regenerated `.sqlx/*.json`. Confirm `SQLX_OFFLINE=true cargo build --all-targets` exits 0 so offline CI stays green. If `cargo sqlx prepare` produces no diff, that is the expected and acceptable outcome — record it in the summary.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo build --all-targets</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `SQLX_OFFLINE=true cargo build --all-targets` exits 0.
    - Behavior: `cargo sqlx prepare -- --all-targets` against a 0002-migrated DB reports no uncommitted drift (clean working tree for `.sqlx/` after commit).
  </acceptance_criteria>
  <done>Offline metadata is verified current and the offline build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live schema | DDL widens a CHECK domain and redefines a public contract view consumed by the separate spam layer |
| test SQL → Postgres | parameterized sqlx queries cross into the DB; never string-formatted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | DDL re-run on a populated DB | mitigate | Idempotent additive migration (named DROP+ADD constraint, ADD COLUMN IF NOT EXISTS); `migrations_idempotent` test proves re-run no-op |
| T-03-02 | Information disclosure | pubkey_freshness contract view | mitigate | `in_progress` collapsed to `discovered`; internal columns absent from the view; verified by `migration_0002_widens_status_and_hides_in_progress` |
| T-03-03 | Tampering | string-formatted SQL injection in tests | mitigate | All test queries parameterized (`$1` binds), per project convention (T-03-01 store rule) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 3 installs no packages (RESEARCH Package Legitimacy Audit: not applicable); no new dependency surface |
</threat_model>

<verification>
- `SQLX_OFFLINE=true cargo test --test migrations` green (idempotency + widened domain + view collapse).
- `SQLX_OFFLINE=true cargo test --no-run --test graph_writer --test frontier` compiles both scaffolds.
- `SQLX_OFFLINE=true cargo build --all-targets` exit 0 (offline metadata committed).
</verification>

<success_criteria>
- `migrations/0002_frontier.sql` is additive, idempotent, widens `status` to include `in_progress`, adds `claimed_at`+`fetch_attempts`, and hides `in_progress` from `pubkey_freshness`.
- The migration test suite proves all of the above.
- `tests/graph_writer.rs` and `tests/frontier.rs` exist as compiling Wave 0 scaffolds naming every Phase 3 requirement's test.
- Offline build is green.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03-01)

- **New file** `migrations/0002_frontier.sql` — additive frontier migration.
- **New SQL constraint** `pubkeys_status_check` (re-created, named) widening `status` domain to include `'in_progress'`.
- **New columns** `pubkeys.claimed_at TIMESTAMPTZ`, `pubkeys.fetch_attempts SMALLINT NOT NULL DEFAULT 0` (both INTERNAL).
- **Redefined view** `pubkey_freshness` (collapses `in_progress` → `discovered`).
- **New file** `tests/graph_writer.rs` — GRAPH-02 test scaffold with stubs `apply_diff_adds_and_removes`, `same_event_zero_touch`, `newest_wins_under_concurrent_apply`.
- **New file** `tests/frontier.rs` — frontier test scaffold with stubs `bfs_reaches_full_component`, `spam_island_never_crawled`, `crash_resume_no_redo`, `startup_reclaims_in_progress`, `bounded_concurrency`, `skip_locked_no_double_claim`, `last_fetched_at_stamped_on_terminal`.
- **Modified** `tests/migrations.rs` — adds `migration_0002_widens_status_and_hides_in_progress`.
- **Regenerated** `.sqlx/` offline metadata (verified, likely no-diff).
</artifacts_produced>

<output>
Create `.planning/phases/03-graph-writer-bfs-frontier/03-01-SUMMARY.md` when done
</output>
