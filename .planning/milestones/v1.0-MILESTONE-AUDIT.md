---
milestone: v1.0
audited: 2026-06-15
status: tech_debt
scores:
  requirements: 29/29
  phases: 5/5
  integration: clean
  flows: 1/1
gaps: {}
nyquist:
  compliant_phases: [02, 03]
  partial_phases: [01, 04, 05]
  missing_phases: []
  overall: partial
tech_debt:
  - phase: 04-daemon-staleness-loop-observability
    items:
      - "Operator UAT deferred (user-accepted): live-relay crawl run + Grafana dashboard render against live Prometheus. Every automatable criterion passed in-session; tracked in 04-UAT.md."
      - "VALIDATION.md nyquist_compliant flag left false (draft) though all phase-4 tasks have green automated tests — flip via /gsd-validate-phase 4 if formal Nyquist sign-off is wanted."
  - phase: 05-nip-65-outbox-routing-relay-health
    items:
      - "Operator UAT (shared with Phase 4): real-relay NIP-65 fallback effectiveness + health-routing under real relay degradation. Automatable mechanics fully tested offline."
      - "VALIDATION.md nyquist_compliant flag left false (draft) though phase-5 tasks have green automated tests."
  - phase: 01-schema-data-contract
    items:
      - "VALIDATION.md nyquist_compliant flag false (carried from initial milestone setup) — phase verified passed; advisory only."
  - phase: cross-cutting
    items:
      - "tests/daemon_config.rs requires `-- --test-threads=1` (an env-var WOT__CONCURRENCY mutation leaks across parallel tests). Production wiring is correct; recommend a per-test unique env var or a serialization mutex, plus CI enforcement of the flag."
      - "Known testcontainers flake (`container ... does not expose port 5432/tcp`) under full-suite parallel load — every test binary passes in isolation / on re-run. Test-infra, not a product defect. Run DB suites per-binary with `-- --test-threads=2`."
      - ".planning/config.json contains 4 unknown keys (tavily_search, ref_search, perplexity, jina) that gsd-tools ignores — harmless config cruft."
---

# Milestone v1.0 — Audit Report

**Status:** tech_debt (all requirements satisfied, integration clean, no blockers; accumulated deferred/advisory items below)

## Definition of Done

From one anchor pubkey, maintain a complete and continuously fresh follow graph of everyone reachable through follows — fetched efficiently (each list roughly once, refreshed only when stale) — persisted in a shared PostgreSQL database the downstream spam layer reads directly, run by a single unattended daemon with observability.

## Requirements Coverage: 29/29 satisfied

All milestone requirements are marked **Complete** in REQUIREMENTS.md, each verified by its phase's VERIFICATION.md and confirmed wired end-to-end by the integration check:

| Group | IDs | Phase | Status |
|-------|-----|-------|--------|
| Schema/Graph | GRAPH-01, GRAPH-03, GRAPH-04 | 1 | satisfied |
| Ingest | INGEST-01..05 | 2 | satisfied |
| Relay acquisition | RELAY-01..04 | 2 | satisfied |
| Graph writer + frontier | GRAPH-02, CRAWL-01..04, FRESH-01 | 3 | satisfied |
| Daemon + staleness + observability | FRESH-02, FRESH-03, OBS-01..05, OPS-01, OPS-02 | 4 | satisfied |
| NIP-65 + relay health | RELAY-05, RELAY-06 | 5 | satisfied |

No orphaned requirements. No unsatisfied requirements. FAIL gate: not triggered.

## Phase Verifications

| Phase | Status | Notes |
|-------|--------|-------|
| 1 Schema & Data Contract | passed | — |
| 2 Relay Acquisition & Validation | passed | — |
| 3 Graph Writer & BFS Frontier | passed | 8/8 must-haves; CR-01 + 3 warnings fixed |
| 4 Daemon, Staleness Loop & Observability | human_needed | 9/9 automatable must-haves passed; 2 operator-UAT items deferred (user-accepted); 2 Critical + 4 warnings fixed |
| 5 NIP-65 Outbox Routing & Relay Health | passed | 9/9 must-haves; 3 Critical + 3 warnings fixed |

Phase 4's `human_needed` is the user-accepted operator-UAT deferral (live-relay run + Grafana), not a requirements gap — all of its requirement checkboxes are Complete.

## Cross-Phase Integration: clean

Integration checker found 0 orphaned exports, 0 missing connections, 0 broken flows. The full E2E flow is wired: `main.rs` → config load+validate → tracing → metrics recorder → PgPool + migrations (0001→0002→0003→0004 clean additive chain) → relay connect + NOTICE consumer → `run_daemon_loop` (seed → reclaim → claim → `process_batch` → `acquire_validated_lists` single ingest → `apply_validated`/`apply_follow_list` edge writes / NIP-65 `fallback_recover` on miss / `not_found`) → staleness + in-run-reclaim + sampler timers → axum `/metrics` + `/health/*` → SIGTERM CancellationToken graceful drain. `cargo build --bin crawler` succeeds.

## Nyquist Compliance (advisory)

| Phase | VALIDATION.md | nyquist_compliant | Note |
|-------|---------------|-------------------|------|
| 1 | exists | false | passed; flag never flipped |
| 2 | exists | true | compliant |
| 3 | exists | true | compliant (signed off) |
| 4 | exists | false | green automated tests; flag left draft |
| 5 | exists | false | green automated tests; flag left draft |

Phases 1/4/5 have automated tests that the verifier confirmed green; only the `nyquist_compliant` frontmatter flag was not flipped. Run `/gsd-validate-phase 4` / `/gsd-validate-phase 5` if formal Nyquist sign-off is desired. Advisory — does not block completion.

## Tech Debt Summary

See frontmatter `tech_debt`. All items are deferred/advisory (operator UAT, Nyquist flags, a test-isolation warning, the testcontainers flake, config cruft) — none are correctness blockers. The product code is complete and verified.
