---
phase: 05-nip-65-outbox-routing-relay-health
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/relay/health.rs
  - src/relay/mod.rs
  - src/daemon/mod.rs
  - src/daemon/config.rs
  - config.example.toml
  - tests/relay_health.rs
  - tests/daemon_config.rs
autonomous: true
requirements: [RELAY-06]
must_haves:
  truths:
    - "Each relay carries a continuous EWMA health score in [0,1] (unknown relay = 1.0)"
    - "A success raises the score (penalized by latency), a timeout/connect-failure drives it toward 0, a rate-limit hit degrades it (not to zero)"
    - "Per-relay permit count scales with health: max(1, round(per_relay_concurrency * score)) so a degraded relay gets fewer (but at least 1)"
    - "A relay below relay_health_threshold is skipped for routing unless a probe is due"
    - "The 5 new config fields fail-fast validate: threshold in [0,1], alpha in (0,1], max_write_relays>0, per_relay_concurrency>0"
    - "spawn_notice_consumer takes an Arc<RelayHealthRegistry> and its daemon callsite constructs+passes one, so the build (incl. tests) stays green at the end of Wave 1"
  artifacts:
    - path: "src/relay/health.rs"
      provides: "RelayHealthRegistry (parallel to RateLimiterRegistry): EWMA score, record_success/timeout/connect_failure/rate_limited, score, permits, in_use, probe bookkeeping; DEFAULT_HEALTH_* consts"
      exports: ["RelayHealthRegistry"]
    - path: "src/daemon/config.rs"
      provides: "nip65_fallback_enabled, nip65_max_write_relays, relay_health_threshold, per_relay_concurrency, health_alpha fields + serde defaults + validate guards + Debug"
      contains: "relay_health_threshold"
    - path: "src/daemon/mod.rs"
      provides: "minimal wiring patch: construct Arc<RelayHealthRegistry> where Arc<RateLimiterRegistry> is built and pass it into spawn_notice_consumer (keeps build green; 05-04 extends this into full routing)"
      contains: "RelayHealthRegistry"
    - path: "config.example.toml"
      provides: "documented defaults for the 5 new fields (keeps example_config_is_valid green)"
    - path: "tests/relay_health.rs"
      provides: "offline EWMA/routing/permit unit tests (created here; owned by this plan)"
  key_links:
    - from: "src/relay/mod.rs"
      to: "RelayHealthRegistry::record_rate_limited"
      via: "handle_relay_message RateLimited arm beside record_notice; spawn_notice_consumer threads Arc<RelayHealthRegistry>"
      pattern: "record_rate_limited"
    - from: "src/daemon/mod.rs"
      to: "spawn_notice_consumer"
      via: "construct Arc<RelayHealthRegistry> beside the rate limiter registry and pass it into the consumer"
      pattern: "RelayHealthRegistry::new"
    - from: "src/daemon/config.rs"
      to: "relay::health::DEFAULT_HEALTH_ALPHA"
      via: "serde default fn references the const by name"
      pattern: "DEFAULT_HEALTH"
---

<objective>
Build the RELAY-06 in-memory `RelayHealthRegistry` (a continuous EWMA per-relay health score in [0,1]) parallel to `RateLimiterRegistry`, wire the rate-limit-hit capture into the existing NOTICE consumer, patch the daemon callsite so the build stays green, and add the 5 new config fields with fail-fast validation. The score/skip/permit math is implemented and unit-tested offline here; the fetch/fan-out capture + full health-driven routing wiring lands in 05-04.

Purpose: The health score is the input every RELAY-06 routing and per-relay-concurrency decision reads. It must exist, be score-correct, and be threaded into the notice consumer (with the daemon callsite patched so Wave 1 ends with a green build) before the fan-out plan consumes it. Config knobs gate behavior and must validate fail-fast (mirroring Phase 4).
Output: `src/relay/health.rs` with `RelayHealthRegistry` + EWMA/permit/probe methods + `DEFAULT_HEALTH_*` consts; the rate-limit-hit hook + threaded param in `src/relay/mod.rs`; the minimal `Arc<RelayHealthRegistry>` construction + callsite patch in `src/daemon/mod.rs`; 5 new fields + guards in `src/daemon/config.rs`; documented defaults in `config.example.toml`; the new offline EWMA/routing/permit unit tests in `tests/relay_health.rs` (this plan creates that file); extended config validation tests. (This plan owns `tests/relay_health.rs`; 05-01 owns `tests/nip65_fallback.rs` — disjoint within Wave 1. 05-01 does NOT touch `src/daemon/mod.rs`, so adding it here introduces no Wave 1 file collision; 05-04 in Wave 3 also owns `src/daemon/mod.rs` — sequential waves sharing the file is fine, and 05-04 extends this construction rather than re-creating it.)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-nip-65-outbox-routing-relay-health/05-RESEARCH.md
@.planning/phases/05-nip-65-outbox-routing-relay-health/05-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RelayHealthRegistry (EWMA score, permits, probe) parallel to RateLimiterRegistry</name>
  <files>src/relay/health.rs, src/relay/mod.rs, tests/relay_health.rs</files>
  <read_first>
    - src/relay/rate_limit.rs lines 1-46 (module header + DEFAULT_* const convention), lines 131-187 (Mutex<HashMap>-behind-Arc struct + clone-out-drop-lock acquire discipline), lines 238-264 (failure_count/active_relay_count/has_limiter introspection style), lines 206-207 (labeled metric, NO manual _total)
    - src/relay/mod.rs (the `pub mod ...;` registration block — add `pub mod health;`)
    - 05-RESEARCH.md Pattern 5 (struct shape, unknown=1.0), Pattern 6 (EWMA math: success sample = 1/(1+latency_secs/latency_scale); timeout/connect=0.0; rate-limit=0.2; score=alpha*sample+(1-alpha)*prev; permits=max(1,round(per_relay_concurrency*score))), Pattern 7 (skip-below-threshold + probe_due), Pitfall 3 (parallel, NOT an extension of the limiter)
    - tests/daemon_config.rs (offline `#[test]` shape) and rate_limit.rs pure-fn tests (backoff_delay_unjittered) for the new tests/relay_health.rs style
    - 05-PATTERNS.md "src/relay/health.rs" section
  </read_first>
  <behavior>
    - new(alpha) starts empty; score(unknown_relay) == 1.0.
    - record_success(relay, fast_latency) moves score toward ~1.0; record_success with large latency yields a sample well below 1.0.
    - record_timeout / record_connect_failure push score toward 0 over repeated calls.
    - record_rate_limited degrades but never zeroes (sample 0.2 floor effect via EWMA).
    - permits(relay) == max(1, round(per_relay_concurrency * score)); a score near 0 still yields 1.
    - route_allowed(relay, threshold) is false when score<threshold and no probe due, true when score>=threshold OR a probe is due; recording an attempt updates last_probe.
  </behavior>
  <action>
    Create src/relay/health.rs with a module header (phase + RELAY-06) mirroring rate_limit.rs. Define `pub const DEFAULT_HEALTH_ALPHA: f64 = 0.3;`, `pub const DEFAULT_RELAY_HEALTH_THRESHOLD: f64 = 0.25;`, `pub const DEFAULT_PER_RELAY_CONCURRENCY: usize = 4;`, `pub const DEFAULT_NIP65_MAX_WRITE_RELAYS: usize = 3;`, `pub const DEFAULT_NIP65_FALLBACK_ENABLED: bool = true;` (conservative/relay-polite per CONTEXT; these back config defaults by name). Add `const LATENCY_SCALE_SECS: f64 = 3.0;` and `const PROBE_INTERVAL: Duration = Duration::from_secs(60);` (Claude's discretion per RESEARCH A1). Define `pub struct RelayHealthRegistry { alpha: f64, scores: Mutex<HashMap<String, f64>>, in_use: Mutex<HashMap<String, u32>>, last_probe: Mutex<HashMap<String, Instant>> }` and `pub fn new(alpha: f64) -> Self`. Implement: `record_success(&self, relay: &str, latency: Duration)` (sample = 1.0/(1.0 + latency.as_secs_f64()/LATENCY_SCALE_SECS)), `record_timeout(&self, relay)` (sample 0.0), `record_connect_failure(&self, relay)` (sample 0.0), `record_rate_limited(&self, relay)` (sample 0.2), all funneling through a private `update(&self, relay, sample)` doing `alpha*sample + (1-alpha)*prev` with `prev` defaulting to 1.0. Implement `pub fn score(&self, relay: &str) -> f64` (unknown = 1.0), `pub fn permits(&self, relay: &str, per_relay_concurrency: usize) -> usize` (= max(1, round(per_relay_concurrency as f64 * score))), `pub fn in_use(&self, relay: &str) -> u32`, plus `pub fn incr_in_use`/`pub fn decr_in_use` for the admission gate (05-04 uses these). Implement `pub fn route_allowed(&self, relay: &str, threshold: f64) -> bool` returning `score >= threshold || last_probe(relay).elapsed() >= PROBE_INTERVAL`, and `pub fn mark_attempt(&self, relay: &str)` updating last_probe to now. Follow the clone-out-drop-lock discipline: scope every `.lock().expect("health map")` to the smallest block; never hold a lock across an await (the updates are synchronous, so this is naturally safe — keep it that way). This is a PARALLEL registry — do NOT modify or extend RateLimiterRegistry (Pitfall 3). Register `pub mod health;` in src/relay/mod.rs. Create the new test file tests/relay_health.rs with offline `#[test]`s (no DB): `ewma_moves_with_signals`, `skip_then_probe`, `permits_scale_with_health`, asserting the behaviors above directly against the public registry API.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo test --test relay_health -- --test-threads=2</automated>
  </verify>
  <acceptance_criteria>
    - `web_of_trust::relay::health::RelayHealthRegistry` exists with record_success/record_timeout/record_connect_failure/record_rate_limited, score, permits, in_use, incr_in_use, decr_in_use, route_allowed, mark_attempt.
    - `DEFAULT_HEALTH_ALPHA`, `DEFAULT_RELAY_HEALTH_THRESHOLD`, `DEFAULT_PER_RELAY_CONCURRENCY`, `DEFAULT_NIP65_MAX_WRITE_RELAYS`, `DEFAULT_NIP65_FALLBACK_ENABLED` are `pub const`.
    - tests/relay_health.rs exists with `ewma_moves_with_signals`, `skip_then_probe`, `permits_scale_with_health` as non-ignored offline tests, all green, asserting: score rises on fast success and falls on timeout/connect/rate-limit; permits == max(1, round(...)); route_allowed flips with threshold + probe.
    - grep confirms rate_limit.rs is NOT modified to carry health state (`git diff --stat` shows src/relay/rate_limit.rs unchanged in this task).
    - No `.lock()` is held across an `.await` in src/relay/health.rs (all updates synchronous).
  </acceptance_criteria>
  <done>RelayHealthRegistry exists as a parallel registry with score-correct EWMA, permit scaling, and probe routing, all green in the new tests/relay_health.rs.</done>
</task>

<task type="auto">
  <name>Task 2: Record rate-limit hits into health via the NOTICE consumer + patch the daemon callsite (keep Wave 1 build green)</name>
  <files>src/relay/mod.rs, src/daemon/mod.rs</files>
  <read_first>
    - src/relay/mod.rs lines 252-263 (handle_relay_message: RateLimited|Blocked arm calling record_notice), lines 280-311 (spawn_notice_consumer threading the Arc<RateLimiterRegistry>)
    - src/daemon/mod.rs lines 163-168 (where Arc<RateLimiterRegistry> is built once), line 168 (the spawn_notice_consumer callsite that currently passes only the rate limiter registry)
    - src/relay/health.rs RelayHealthRegistry::new + record_rate_limited (from Task 1)
    - 05-RESEARCH.md "Threading the new registry" + Code Examples (record rate-limit hit beside record_notice), Pitfall 3 (touch src/relay/mod.rs only for the one hook + threaded param)
    - 05-PATTERNS.md "src/relay/mod.rs" section
  </read_first>
  <action>
    Two coordinated edits so the build (incl. tests) is GREEN at the end of Wave 1:
    (1) src/relay/mod.rs — thread an `Arc<RelayHealthRegistry>` into `spawn_notice_consumer` (a new param, cloned into the consumer exactly as the existing `registry: Arc<RateLimiterRegistry>` is). In `handle_relay_message`, in the `NoticeKind::RateLimited` arm (beside the existing `registry.record_notice(...)`), call `health.record_rate_limited(relay_url)`. Touch src/relay/mod.rs ONLY for this hook + the threaded param + any signature change it requires — do NOT entangle health into the rate limiter (Pitfall 3).
    (2) src/daemon/mod.rs — patch the now-broken `spawn_notice_consumer` callsite. Where the existing `Arc<RateLimiterRegistry>` is constructed (lines 163-168), ALSO construct `let health = Arc::new(RelayHealthRegistry::new(cfg.health_alpha));` (use the new config field from Task 3; if Task 3 is sequenced after this within the plan, default to `RelayHealthRegistry::new(crate::relay::health::DEFAULT_HEALTH_ALPHA)` and let Task 3 swap it to `cfg.health_alpha`). Pass `Arc::clone(&health)` into the `spawn_notice_consumer(...)` call so its new signature is satisfied. This is a MINIMAL wiring patch only — full health-driven routing, per-relay semaphore admission, and fetch-arm capture all land in 05-04, which will reuse this exact `health` binding (extend, do not duplicate the construction). Keep the binding named `health` so 05-04 can thread it into the fan-out and sampler without renaming.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo build --tests</automated>
  </verify>
  <acceptance_criteria>
    - `spawn_notice_consumer` signature includes an `Arc<RelayHealthRegistry>` param; the RateLimited arm of handle_relay_message calls `record_rate_limited`.
    - grep `record_rate_limited` in src/relay/mod.rs returns a match inside the RateLimited handling.
    - src/daemon/mod.rs constructs `Arc::new(RelayHealthRegistry::new(...))` next to the rate limiter registry and passes `Arc::clone(&health)` into `spawn_notice_consumer` (grep `RelayHealthRegistry::new` in src/daemon/mod.rs returns a match).
    - `SQLX_OFFLINE=true cargo build --tests` succeeds (the daemon callsite is patched — no signature mismatch left).
    - rate_limit.rs is not modified in this task.
  </acceptance_criteria>
  <done>The NOTICE consumer records a rate-limit hit into RelayHealthRegistry AND the daemon callsite constructs+passes the registry, so the Wave 1 build (incl. tests) is green.</done>
</task>

<task type="auto">
  <name>Task 3: 5 new config fields + fail-fast validate + config.example.toml + tests</name>
  <files>src/daemon/config.rs, config.example.toml, tests/daemon_config.rs</files>
  <read_first>
    - src/daemon/config.rs lines 48-99 (Config fields), lines 101-137 (default_* fns referencing library consts by name), lines 143-165 (hand-impl Debug — add every new field or the redaction test drifts), lines 194-215 (validate with anyhow::ensure! numeric guards)
    - src/relay/health.rs (the DEFAULT_HEALTH_* consts from Task 1, referenced by name)
    - tests/daemon_config.rs lines 133 (example_config_is_valid), lines 153-193 (*_zero_rejected idiom; this suite mutates env -> runs --test-threads=1)
    - 05-RESEARCH.md "Config additions" (5 fields + guards) and 05-PATTERNS.md "src/daemon/config.rs" section
  </read_first>
  <action>
    Add 5 fields to the Config struct: `nip65_fallback_enabled: bool`, `nip65_max_write_relays: usize`, `relay_health_threshold: f64`, `per_relay_concurrency: usize`, `health_alpha: f64`. Each gets `#[serde(default = "default_<name>")]` and a matching `fn default_<name>()` that references the `relay::health::DEFAULT_*` const by name (never re-literal). Add each new field to the hand-impl `Debug` (none are secrets — render them, but they MUST appear so the redaction test does not drift). In `validate`, add `anyhow::ensure!` guards: `nip65_max_write_relays > 0` ("nip65_max_write_relays must be > 0"), `per_relay_concurrency > 0` ("per_relay_concurrency must be > 0"), `(0.0..=1.0).contains(&relay_health_threshold)` ("relay_health_threshold must be in [0,1]"), `health_alpha > 0.0 && health_alpha <= 1.0` ("health_alpha must be in (0,1]"). Document all 5 fields with their default values in config.example.toml (keeps the example_config_is_valid test green). Never log database_url (unchanged invariant). Once this field exists, ensure the daemon (Task 2 edit) uses `cfg.health_alpha` rather than the const fallback.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo test --test daemon_config -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Config has all 5 new fields with serde defaults referencing relay::health::DEFAULT_* consts by name (grep `DEFAULT_HEALTH`/`DEFAULT_RELAY_HEALTH`/`DEFAULT_PER_RELAY`/`DEFAULT_NIP65` in config.rs).
    - All 5 fields appear in the hand-impl Debug.
    - validate rejects: nip65_max_write_relays=0, per_relay_concurrency=0, relay_health_threshold=1.5 (and <0), health_alpha=0.0 (and >1.0) — each with the named error substring.
    - config.example.toml documents the 5 fields and example_config_is_valid stays green.
    - tests/daemon_config.rs has new reject-tests for each guard (copy the *_zero_rejected idiom) and they pass under --test-threads=1.
  </acceptance_criteria>
  <done>The 5 config fields exist with const-backed defaults, fail-fast guards, documented example, and green validation tests.</done>
</task>

</tasks>

<verification>
- `SQLX_OFFLINE=true cargo build --tests` green (daemon callsite patched — no signature mismatch after Wave 1).
- `SQLX_OFFLINE=true cargo test --test relay_health -- --test-threads=2` green (offline, no DB).
- `SQLX_OFFLINE=true cargo test --test daemon_config -- --test-threads=1` green.
- src/relay/rate_limit.rs unchanged (Pitfall 3 — health is parallel).
</verification>

<success_criteria>
- A continuous per-relay EWMA health score in [0,1] exists, moves correctly with success/latency/timeout/connect-failure/rate-limit signals, and exposes permit-scaling + probe-routing helpers (RELAY-06 score + the inputs to routing/concurrency).
- The existing NOTICE consumer records rate-limit hits into the health registry, and the daemon callsite constructs+passes the registry so Wave 1 ends with a green build.
- The 5 new config knobs validate fail-fast with conservative defaults.
</success_criteria>

<output>
Create `.planning/phases/05-nip-65-outbox-routing-relay-health/05-02-SUMMARY.md` when done.
</output>
