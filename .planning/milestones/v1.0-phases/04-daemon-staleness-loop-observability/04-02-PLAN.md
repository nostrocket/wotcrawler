---
phase: 04-daemon-staleness-loop-observability
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/daemon/config.rs
  - src/daemon/mod.rs
  - config.example.toml
  - tests/daemon_config.rs
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "The daemon loads its full tunable set from a TOML file given by --config"
    - "WOT__* env vars with __ nesting override file values"
    - "Omitted optional fields fall back to the existing DEFAULT_* constants"
    - "Invalid config (bad anchor, empty relays, unparseable DB URL, TTL=0) fails fast with a clear error before any crawl work"
    - "The database_url is never logged"
  artifacts:
    - path: "src/daemon/config.rs"
      provides: "Config struct + load_config + validate"
      contains: "pub struct Config"
    - path: "config.example.toml"
      provides: "Documented example config covering every field"
      contains: "anchor_pubkey"
    - path: "tests/daemon_config.rs"
      provides: "Config load/override/default/validation tests"
      contains: "override_precedence"
  key_links:
    - from: "src/daemon/config.rs"
      to: "crate::crawl::DEFAULT_BATCH_SIZE / DEFAULT_CONCURRENCY / DEFAULT_MAX_ATTEMPTS"
      via: "serde default fns returning the existing consts"
      pattern: "DEFAULT_"
    - from: "src/daemon/config.rs::validate"
      to: "nostr_sdk::PublicKey"
      via: "anchor pubkey hex/bech32 parse"
      pattern: "PublicKey::(parse|from_hex|from_bech32)"
---

<objective>
Build the daemon's configuration front-end (OPS-01): a serde `Config` struct loaded from a TOML file via the `config` crate with `--config` and `WOT__*` env overlay, every optional field defaulting to the existing `DEFAULT_*` constants, fail-fast `validate()` for the anchor pubkey / relay set / DB URL / TTL, a committed `config.example.toml` documenting every field, and unit tests proving load precedence + defaults + validation.

Purpose: One config struct is the single source of truth every other daemon task consumes (loop sizing, bind addresses, intervals, log format).
Output: `src/daemon/config.rs`, `config.example.toml`, and green `tests/daemon_config.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-daemon-staleness-loop-observability/04-CONTEXT.md
@.planning/phases/04-daemon-staleness-loop-observability/04-RESEARCH.md
@.planning/phases/04-daemon-staleness-loop-observability/04-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- `src/daemon/config.rs` — `pub struct Config` with fields: `anchor_pubkey: String`, `relays: Vec<String>`, `database_url: String`, `ttl` (humantime Duration), `concurrency: usize` (default `DEFAULT_CONCURRENCY`), `batch_size: i64` (default `DEFAULT_BATCH_SIZE`), `max_attempts: i16` (default `DEFAULT_MAX_ATTEMPTS`), `fetch_timeout` (humantime, default `DEFAULT_FETCH_TIMEOUT`), `reqs_per_second: u32` (default `DEFAULT_REQS_PER_SECOND`), `metrics_addr: SocketAddr`, `log_level: String` (default `"info"`), `log_format: LogFormat` (`Human`|`Json`, default `Human`), `progress_interval`, `staleness_scan_interval`, `reclaim_interval`, `reclaim_age` (in-run lease threshold), `idle_poll_interval` (all humantime Durations). `pub enum LogFormat { Human, Json }`. `pub fn load_config(path: &str) -> anyhow::Result<Config>` and `pub fn validate(&Config) -> anyhow::Result<()>`.
- `config.example.toml` — every field with a documented comment and a sensible default value.
- `tests/daemon_config.rs` — fills the Wave 0 stub tests.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Config struct + load + validate</name>
  <files>src/daemon/config.rs, src/daemon/mod.rs</files>
  <read_first>
    - 04-RESEARCH.md §Code Examples "Config struct + load + validate" (the `config::Config::builder().add_source(File).add_source(Environment::default().prefix("WOT").separator("__"))` + `humantime_serde` + `validate()` pattern — follow verbatim) and §Assumptions A2 (confirm `nostr_sdk::PublicKey::parse` accepts hex AND bech32 in 0.44; `from_hex`/`from_bech32` are the fallbacks)
    - 04-PATTERNS.md §"src/daemon/config.rs" (style-only analog; every `#[serde(default = "...")]` returns the existing `DEFAULT_*` const — do NOT re-literal the numbers; never log `database_url`)
    - src/crawl/mod.rs:33-59 (`DEFAULT_BATCH_SIZE=64`, `DEFAULT_CONCURRENCY=8`, `DEFAULT_MAX_ATTEMPTS=3` — the consts the defaults reference)
    - src/relay/rate_limit.rs:37-46 (`DEFAULT_REQS_PER_SECOND`) and src/relay/fetch.rs:31-32 (`DEFAULT_FETCH_TIMEOUT`)
    - src/store/mod.rs:24-29 (`MAX_CONNECTIONS` is private; `connect` doc warns "never log database_url")
    - 04-RESEARCH.md §Common Pitfalls Pitfall 5 (humantime serde wiring) + §Security Domain (V5 fail-fast validation)
  </read_first>
  <action>
    Create `src/daemon/config.rs` with a `//!` header citing OPS-01. Define `pub enum LogFormat { Human, Json }` with `#[derive(Deserialize, Default)]` (default `Human`) and serde rename to lowercase. Define `pub struct Config` (derive `Debug, Deserialize`) with the field set listed in <artifacts_this_phase_produces>. Duration fields use `#[serde(with = "humantime_serde")]`; optional fields use `#[serde(default = "default_*")]` where each `default_*` fn returns the existing `crate::crawl::DEFAULT_*` / `crate::relay::rate_limit::DEFAULT_*` / fetch-timeout const (reference the const by name — do NOT inline the literal). Implement `pub fn load_config(path: &str) -> anyhow::Result<Config>` using `config::Config::builder().add_source(config::File::with_name(path)).add_source(config::Environment::default().prefix("WOT").separator("__")).build()?.try_deserialize()`. Implement `pub fn validate(c: &Config) -> anyhow::Result<()>`: parse the anchor via `nostr_sdk::PublicKey::parse` (fall back to `from_hex`/`from_bech32` if `parse` is not the accepting method in 0.44 — confirm against the SDK) returning a clear `anyhow!("invalid anchor_pubkey: ...")` on failure; `anyhow::ensure!(!c.relays.is_empty(), "relays must be non-empty")`; `anyhow::ensure!(c.ttl > Duration::ZERO, "ttl must be > 0")`; check `database_url` is non-empty and looks like a URL (authoritative parse is the PgPool connect at startup). Provide a method or helper that yields a redacted Debug for config-echo logging that NEVER includes `database_url` (Security Domain T-03-04). Declare `pub mod config;` in src/daemon/mod.rs.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo build --lib 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'pub struct Config' src/daemon/config.rs`, `grep -n 'pub fn load_config' src/daemon/config.rs`, `grep -n 'pub fn validate' src/daemon/config.rs` all match.
    - `grep -n 'DEFAULT_CONCURRENCY\|DEFAULT_BATCH_SIZE\|DEFAULT_MAX_ATTEMPTS' src/daemon/config.rs` matches (defaults reference consts, not literals).
    - `grep -n 'humantime_serde' src/daemon/config.rs` matches on the duration fields.
    - `grep -n 'prefix("WOT")' src/daemon/config.rs` and `grep -n 'separator("__")' src/daemon/config.rs` match.
    - `SQLX_OFFLINE=true cargo build --lib` exits 0.
  </acceptance_criteria>
  <done>Config struct loads TOML + env overlay, defaults reference the existing constants, and validate() fails fast on bad anchor / empty relays / TTL=0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: config.example.toml + daemon_config tests</name>
  <files>config.example.toml, tests/daemon_config.rs</files>
  <behavior>
    - default_fill: a minimal TOML with only the required fields (anchor_pubkey, relays, database_url, ttl, metrics_addr) deserializes and every omitted optional field equals its `DEFAULT_*` const (concurrency==8, batch_size==64, max_attempts==3, log_level=="info", log_format==Human).
    - override_precedence: with the same TOML on disk, setting env `WOT__CONCURRENCY=16` (and `WOT__LOG_FORMAT=json`) makes the loaded Config report concurrency==16 and log_format==Json (env beats file).
    - invalid_anchor_rejected: a TOML whose anchor_pubkey is `"not-a-key"` makes `validate()` return Err with a message containing "anchor".
    - ttl_zero_rejected: a TOML with `ttl = "0s"` makes `validate()` return Err with a message containing "ttl".
    - empty_relays_rejected: a TOML with `relays = []` makes `validate()` return Err.
  </behavior>
  <read_first>
    - 04-RESEARCH.md §Validation Architecture "Phase Requirements → Test Map" OPS-01 row (the exact test names: `override_precedence default_fill invalid_anchor_rejected ttl_zero_rejected`) and §Test Seams "humantime-serde is pure-unit"
    - tests/daemon_config.rs (the named `#[ignore]` stubs created in 04-01 — fill them)
    - tests/migrations.rs (pure-assertion test structure analog — no DB)
    - 04-CONTEXT.md §Configuration (the full tunable set + example/committed config requirement)
  </read_first>
  <action>
    Create `config.example.toml` documenting EVERY Config field with an inline `#` comment and a sensible value: a placeholder `anchor_pubkey`, a small curated `relays` array, a `database_url` placeholder (with a comment that it is never logged), `ttl = "24h"`, all interval fields (`staleness_scan_interval`, `reclaim_interval`, `reclaim_age`, `idle_poll_interval`, `progress_interval`) with example human durations, `metrics_addr = "127.0.0.1:9100"` (with a comment that binding to a public address exposes internal topology — Security Domain), `concurrency`/`batch_size`/`max_attempts`/`fetch_timeout`/`reqs_per_second` showing their defaults, and `log_level`/`log_format`. Fill `tests/daemon_config.rs`: convert the `#[ignore]` stubs into real `#[test]` (no DB needed) tests implementing the five behaviors in `<behavior>`. Write each test's TOML to a tempfile (use a `std::env::temp_dir()` path or the `tempfile` dev-dep if already present — otherwise a unique path under temp_dir) and call `load_config` + `validate`. For `override_precedence`, set the env var with `std::env::set_var` (and remove it after) BEFORE `load_config`; run that test serially via a `#[serial]`-style guard or by naming it so the suite is run single-threaded — config env tests must not race, so document running `tests/daemon_config` with `-- --test-threads=1` if env mutation is involved. Add a `dashboard_json_valid`-unrelated note only if needed — this suite is config-only.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo test --test daemon_config -- --test-threads=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/daemon_config.rs` no longer contains `#[ignore]` and defines `default_fill`, `override_precedence`, `invalid_anchor_rejected`, `ttl_zero_rejected`, `empty_relays_rejected`; all pass.
    - `config.example.toml` contains every Config field name (`grep -c 'anchor_pubkey\|relays\|database_url\|ttl\|metrics_addr\|concurrency\|batch_size\|max_attempts\|fetch_timeout\|reqs_per_second\|log_level\|log_format\|staleness_scan_interval\|reclaim_interval\|reclaim_age\|idle_poll_interval\|progress_interval' config.example.toml` matches all fields).
    - `SQLX_OFFLINE=true cargo test --test daemon_config -- --test-threads=1` exits 0.
  </acceptance_criteria>
  <done>Config load precedence, default fill, and fail-fast validation are proven by green unit tests; config.example.toml documents the complete tunable set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config file / env → daemon process | Untrusted-ish operator input (anchor, relays, DB URL) crosses into the process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | database_url in logs / config echo | mitigate | Redacted Debug helper; never place `database_url` in a tracing field (Security Domain T-03-04) |
| T-04-04 | Tampering / DoS | malformed config silently accepted (bad anchor, TTL=0) | mitigate | Fail-fast `validate()` exits non-zero before any crawl work (V5 input validation) |
</threat_model>

<verification>
- `SQLX_OFFLINE=true cargo test --test daemon_config -- --test-threads=1` green.
- `SQLX_OFFLINE=true cargo build --all-targets` exits 0.
- `cargo clippy --all-targets` no new warnings on `src/daemon/config.rs`.
- `grep -c 'database_url' src/daemon/config.rs` shows it appears only in the struct field + non-logging paths (manual review: no tracing field carries it).
</verification>

<success_criteria>
- Config loads from TOML with `--config`, `WOT__*` env overrides win, omitted optionals fall back to `DEFAULT_*`.
- Invalid config fails fast with an actionable message.
- `config.example.toml` documents every field.
</success_criteria>

<output>
Create `.planning/phases/04-daemon-staleness-loop-observability/04-02-SUMMARY.md` when done.
</output>
