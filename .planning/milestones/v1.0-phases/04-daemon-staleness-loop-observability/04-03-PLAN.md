---
phase: 04-daemon-staleness-loop-observability
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/daemon/observe.rs
  - src/daemon/mod.rs
  - ops/grafana-dashboard.json
  - tests/observe.rs
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03, OBS-05]
must_haves:
  truths:
    - "A single axum server serves GET /metrics, /health/live, and /health/ready"
    - "/metrics renders the Prometheus exposition from the installed recorder, including the existing counters once any fire"
    - "/health/live returns 200 unconditionally; /health/ready returns 200 only when the loop is alive AND the DB is reachable, else 503"
    - "Tracing initializes with an EnvFilter and selectable human/JSON format"
    - "A Grafana dashboard JSON committed under ops/ references every OBS-01 metric series"
  artifacts:
    - path: "src/daemon/observe.rs"
      provides: "tracing init, recorder install, axum router (/metrics + /health/*)"
      contains: "PrometheusBuilder"
    - path: "ops/grafana-dashboard.json"
      provides: "OBS-05 dashboard"
      contains: "frontier_depth"
    - path: "tests/observe.rs"
      provides: "metrics render, health semantics, json format, dashboard valid"
      contains: "metrics_endpoint_exposes_series"
  key_links:
    - from: "src/daemon/observe.rs::metrics_handler"
      to: "PrometheusHandle::render"
      via: "axum GET /metrics handler"
      pattern: "\\.render\\(\\)"
    - from: "src/daemon/observe.rs::ready_handler"
      to: "pool + loop_alive flag"
      via: "SELECT 1 ping + AtomicBool"
      pattern: "SERVICE_UNAVAILABLE"
---

<objective>
Build the observability surface (OBS-01/02/03/05): a single `axum` router serving `/metrics` (rendering the installed `metrics-exporter-prometheus` handle), `/health/live`, and `/health/ready`; `tracing` init with an `EnvFilter` and switchable human/JSON layer; named gauges/histograms (frontier depth, coverage, fetch rate, staleness distribution, relay health) registered as constants for the sampler (04-04) to emit into; and a committed Grafana dashboard JSON. The six existing `metrics::counter!` sites become live the instant the recorder is installed — no edits to them.

Purpose: This is the operator's trust surface for an unattended multi-day run.
Output: `src/daemon/observe.rs`, `ops/grafana-dashboard.json`, and green `tests/observe.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-daemon-staleness-loop-observability/04-CONTEXT.md
@.planning/phases/04-daemon-staleness-loop-observability/04-RESEARCH.md
@.planning/phases/04-daemon-staleness-loop-observability/04-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- `src/daemon/observe.rs` — `pub fn install_metrics() -> PrometheusHandle` (installs the GLOBAL recorder + sets histogram buckets), `pub fn init_tracing(level: &str, format: LogFormat)`, `pub struct AppState { handle: PrometheusHandle, loop_alive: Arc<AtomicBool>, pool: PgPool }`, `pub fn router(state: AppState) -> axum::Router`, handlers `metrics_handler`, `live_handler`, `ready_handler`. Exported metric-name constants: `METRIC_FRONTIER_DEPTH = "frontier_depth"`, `METRIC_COVERAGE = "crawl_coverage_ratio"`, `METRIC_FETCH_RATE`/`"fetch_duration_seconds"` histogram, `METRIC_STALENESS_BUCKETS = "staleness_age_seconds"`, `METRIC_RELAY_FAILURES = "relay_consecutive_failures"`, `METRIC_RELAY_ACTIVE = "relay_active_count"` (exact names at Claude's discretion per CONTEXT; document them).
- `ops/grafana-dashboard.json` — panels rendering every OBS-01 series.
- `tests/observe.rs` — fills the Wave 0 stubs for OBS-01/02/03/05 (the OBS-04 progress test is filled in 04-04).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: observe.rs — recorder install, tracing init, axum router + handlers</name>
  <files>src/daemon/observe.rs, src/daemon/mod.rs</files>
  <read_first>
    - 04-RESEARCH.md §Pattern 1 (install the global recorder; render from axum; `install_recorder()` returns `PrometheusHandle`; `set_buckets_for_metric` for histograms; the six existing counter sites become live with NO edits), §Pattern 4 (readiness = `Arc<AtomicBool>` + `SELECT 1` ping), §Pattern 5 (switchable fmt/JSON EnvFilter), §Assumptions A1/A4/A5 (confirm `PrometheusHandle::render` + `install_recorder`/`set_buckets` available with `default-features = false`)
    - src/relay/rate_limit.rs:206-213 (the existing `metrics::counter!("relay_rate_limited", "relay" => ...).increment(1)` sites — emit style to mirror; do NOT add per-pubkey labels — Pitfall 7), and rate_limit.rs:238-254 (`failure_count`/`active_relay_count` — the relay-health metric source the sampler reads in 04-04)
    - src/store/mod.rs:24-29 (PgPool; never log database_url) and the `LogFormat` enum from src/daemon/config.rs (04-02) — `observe::init_tracing` takes it
    - 04-RESEARCH.md §Common Pitfalls Pitfall 1 (recorder before first metric), Pitfall 8 (slow /health/ready ping stalls graceful shutdown — bound the ping with a short timeout), §Security Domain (default-bind metrics_addr to loopback — that binding lives in 04-05; observe.rs just exposes the router)
  </read_first>
  <action>
    Create `src/daemon/observe.rs` with a `//!` header citing OBS-01/OBS-02/OBS-03. `install_metrics()` builds `PrometheusBuilder::new()`, calls `.set_buckets_for_metric(Matcher::Full("fetch_duration_seconds".into()), &[...])` (pick latency buckets) and a staleness-age bucket set, then `.install_recorder()` returning the `PrometheusHandle`; document that this MUST run before any metric fires (Pitfall 1). `init_tracing(level, format)` builds `EnvFilter::try_from_default_env().unwrap_or_else(|_| EnvFilter::new(level))` then a `tracing_subscriber::registry().with(filter)` finished with `fmt::layer().json()` for `LogFormat::Json` or `fmt::layer()` for `Human`, calling `.init()`. Define `pub struct AppState` holding the `PrometheusHandle`, an `Arc<AtomicBool>` `loop_alive`, and the `PgPool` (derive `Clone`). `router(state)` returns a `Router` with `.route("/metrics", get(metrics_handler))`, `.route("/health/live", get(live_handler))`, `.route("/health/ready", get(ready_handler))`, `.with_state(state)`. `metrics_handler(State(st))` returns `st.handle.render()` (a `String`). `live_handler` returns `StatusCode::OK` unconditionally. `ready_handler(State(st))`: if `!st.loop_alive.load(Relaxed)` return `SERVICE_UNAVAILABLE`; else `tokio::time::timeout(short, sqlx::query_scalar!("SELECT 1").fetch_one(&st.pool))` → `OK` on Ok, `SERVICE_UNAVAILABLE` on Err/timeout (Pitfall 8 bounded ping). Declare exported metric-name `pub const`s for the sampler to use. Add `pub mod observe;` to src/daemon/mod.rs. Regenerate `.sqlx` for the `SELECT 1` query (`cargo sqlx prepare`) and commit it. Use `build_recorder()` (NOT global install) where tests need a handle (note this for Task 2). Do NOT add a second hyper server / `http-listener` (Pattern 1 anti-pattern).
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo build --lib 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'pub fn install_metrics' src/daemon/observe.rs`, `grep -n 'pub fn init_tracing' src/daemon/observe.rs`, `grep -n 'pub fn router' src/daemon/observe.rs` all match.
    - `grep -n '\.render()' src/daemon/observe.rs` matches in `metrics_handler`; `grep -n 'SERVICE_UNAVAILABLE' src/daemon/observe.rs` matches in `ready_handler`.
    - `grep -n 'install_recorder' src/daemon/observe.rs` matches and `grep -c 'http-listener' src/daemon/observe.rs Cargo.toml` returns 0.
    - `grep -n '/health/live\|/health/ready\|/metrics' src/daemon/observe.rs` shows all three routes.
    - `SQLX_OFFLINE=true cargo build --lib` exits 0 (`.sqlx` regenerated for SELECT 1).
  </acceptance_criteria>
  <done>One axum router serves /metrics + /health/*; recorder install + tracing init are functions main can call; metric-name constants exported for the sampler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: observe tests + Grafana dashboard JSON</name>
  <files>tests/observe.rs, ops/grafana-dashboard.json</files>
  <behavior>
    - metrics_endpoint_exposes_series: using `PrometheusBuilder::build_recorder()` (local handle, NOT global install), fire a `counter!`/`gauge!`/`histogram!` for the exported metric names within that recorder's scope, then assert `handle.render()` is non-empty and contains each expected series name.
    - live_always_ok: `live_handler` (or `router().oneshot(Request::get("/health/live"))`) returns 200.
    - ready_requires_db_and_loop: with `loop_alive=false`, `/health/ready` returns 503; with `loop_alive=true` + a live testcontainers pool, returns 200; with `loop_alive=true` + a closed/unreachable pool, returns 503.
    - json_format_selected: `init_tracing("info", LogFormat::Json)` and `init_tracing("debug", LogFormat::Human)` each build a subscriber without panicking (assert the builder path for both formats; do not assert global init twice in one process — guard with build-only where possible).
    - dashboard_json_valid: parse `ops/grafana-dashboard.json` as JSON (serde_json) and assert each OBS-01 metric-name constant appears somewhere in the file text.
  </behavior>
  <read_first>
    - 04-RESEARCH.md §Test Seams (use `build_recorder()` not global install for assertion tests; hit axum handlers in-process via `router.oneshot(...)` using `tower::ServiceExt`; recorder is global so install-once — prefer local handle), §Validation Architecture OBS-01/02/03/05 rows (exact test names)
    - tests/observe.rs (the `#[ignore]` stubs from 04-01 to fill)
    - tests/frontier.rs:103-111 (`fresh_db` helper for the ready-with-DB case) and tests/common/mod.rs (`start_postgres`)
    - src/daemon/observe.rs (the router/handlers/constants from Task 1)
    - 04-CONTEXT.md §Observability + §specifics (dashboard renders the OBS-01 metric set: coverage, staleness distribution, relay health, frontier depth, fetch rate, validation failures)
  </read_first>
  <action>
    Create `ops/grafana-dashboard.json` as a valid Grafana dashboard with one panel per OBS-01 series: crawl coverage (`crawl_coverage_ratio`), staleness distribution (`staleness_age_seconds`), relay health (`relay_consecutive_failures` / `relay_active_count`), frontier depth (`frontier_depth`), fetch rate (`fetch_duration_seconds`), and validation-failure counters (reference the existing counter names from `ingest/verify.rs`/`ingest/replaceable.rs`/`ingest/follow_list.rs` and `relay/rate_limit.rs` — read those sites to get exact names). Each panel's `targets[].expr` references the metric name so the `dashboard_json_valid` test's text-contains assertion passes. Fill `tests/observe.rs`: convert the `#[ignore]` stubs into the five real tests in `<behavior>`. Use `tower::ServiceExt::oneshot` against `observe::router(state)` for the health tests; for `metrics_endpoint_exposes_series` use `PrometheusBuilder::build_recorder()` + `metrics::with_local_recorder` (or the recorder's `Recorder` API) so the global install is untouched and tests don't conflict. Run DB-touching tests with `-- --test-threads=2`.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo test --test observe -- --test-threads=2 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `tests/observe.rs` defines `metrics_endpoint_exposes_series`, `live_always_ok`, `ready_requires_db_and_loop`, `json_format_selected`, `dashboard_json_valid` with no `#[ignore]` on them; all pass (`-- --test-threads=2`, re-run once on container race).
    - `ops/grafana-dashboard.json` parses as JSON and `grep -c 'frontier_depth\|crawl_coverage_ratio\|staleness_age_seconds\|relay_consecutive_failures\|fetch_duration_seconds' ops/grafana-dashboard.json` matches every OBS-01 series name.
    - `SQLX_OFFLINE=true cargo test --test observe -- --test-threads=2` exits 0.
  </acceptance_criteria>
  <done>Metrics render, health-endpoint semantics, tracing format selection, and dashboard validity are all proven green; the Grafana JSON references every OBS-01 series.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prometheus scraper / operator → /metrics + /health | Network clients hit the observability HTTP server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | /metrics exposes relay urls + coverage to the public internet | mitigate | Bind addr defaults to loopback in config (04-02/04-05); observe.rs adds no auth by design on a trusted bind (Security Domain V4) |
| T-04-06 | DoS | high-cardinality metric labels (per-pubkey) OOM the recorder/scraper | mitigate | Aggregate gauges only; relay labels bounded by curated set; no per-pubkey labels (Pitfall 7) |
| T-04-07 | DoS (self) | slow /health/ready DB ping stalls graceful shutdown | mitigate | `ready_handler` bounds the `SELECT 1` ping with a short `tokio::time::timeout` (Pitfall 8) |
</threat_model>

<verification>
- `SQLX_OFFLINE=true cargo test --test observe -- --test-threads=2` green.
- `SQLX_OFFLINE=true cargo build --all-targets` exits 0.
- `cargo clippy --all-targets` no new warnings on `src/daemon/observe.rs`.
- `ops/grafana-dashboard.json` is valid JSON (the `dashboard_json_valid` test parses it).
</verification>

<success_criteria>
- One axum server exposes /metrics + /health/live + /health/ready with correct semantics.
- Tracing init supports configurable level + human/JSON format.
- Grafana dashboard JSON committed and references every OBS-01 series.
</success_criteria>

<output>
Create `.planning/phases/04-daemon-staleness-loop-observability/04-03-SUMMARY.md` when done.
</output>
